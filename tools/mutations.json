[
 {
  "name": "route()'s default policy drifts from safe to trusting",
  "file": "inspeximus/core.py",
  "old": "policy: str = \"safe\"",
  "new": "policy: str = \"trusting\"",
  "tests": [
   "tests/test_echo_policy_panel.py"
  ]
 },
 {
  "name": "the docstring number is edited away from the measurement",
  "file": "inspeximus/core.py",
  "old": "1.00 echo-blocked",
  "new": "0.50 echo-blocked",
  "tests": [
   "tests/test_echo_policy_panel.py"
  ]
 },
 {
  "name": "the citation points back at the unrunnable probe",
  "file": "inspeximus/core.py",
  "old": "probes/echo_policy_panel.py",
  "new": "probes/echo_attack_probe_v2.py",
  "tests": [
   "tests/test_echo_policy_panel.py"
  ]
 },
 {
  "name": "the echo guard stops blocking a replayed superseded value",
  "file": "probes/echo_policy_panel.py",
  "old": "m.echo_guard = True",
  "new": "m.echo_guard = False",
  "tests": [
   "tests/test_echo_policy_panel.py"
  ]
 },
 {
  "name": "the content check stops seeing mismatches",
  "file": "inspeximus/audit_bundle.py",
  "old": "mismatched = b.get(\"mismatched\") or []",
  "new": "mismatched = []",
  "tests": [
   "tests/test_verify_bundle_content_scope.py"
  ]
 },
 {
  "name": "the verdict claims content was checked when it was not",
  "file": "inspeximus/audit_bundle.py",
  "old": "content_checked = store_items is not None",
  "new": "content_checked = True",
  "tests": [
   "tests/test_verify_bundle_content_scope.py"
  ]
 },
 {
  "name": "ordinary growth is escalated into a failure (false alarm)",
  "file": "inspeximus/audit_bundle.py",
  "old": "limits.append(f\"record {mid} is covered by no receipt in this bundle (written after it was \"",
  "new": "bad(f\"record {mid} is covered by no receipt in this bundle (written after it was \"",
  "tests": [
   "tests/test_verify_bundle_content_scope.py"
  ]
 },
 {
  "name": "the CLI stops printing the scope next to the verdict",
  "file": "inspeximus/cli.py",
  "old": "            for lim in res.get(\"limits\") or []:\n                print(f\"  NOTE {lim}\")\n            s = res[\"summary\"]",
  "new": "            for lim in []:\n                print(f\"  NOTE {lim}\")\n            s = res[\"summary\"]",
  "tests": [
   "tests/test_verify_bundle_content_scope.py"
  ]
 },
 {
  "name": "the headline CLI stops passing the store through",
  "file": "inspeximus/cli.py",
  "old": "res = verify_bundle(bundle, witnesses=wl, threshold=a.threshold,\n                            store_items=items, store_receipts=receipts,",
  "new": "res = verify_bundle(bundle, witnesses=wl, threshold=a.threshold,\n                            store_receipts=receipts,",
  "tests": [
   "tests/test_verify_bundle_content_scope.py"
  ]
 },
 {
  "name": "a mistyped store path is created instead of refused (shared guard)",
  "file": "inspeximus/audit_bundle.py",
  "old": "    if not os.path.exists(path):\n        return None\n    return list(Inspeximus(path=path, receipts=True).items)",
  "new": "    if False:\n        return None\n    return list(Inspeximus(path=path, receipts=True).items)",
  "tests": [
   "tests/test_docs_examples_are_runnable.py"
  ]
 },
 {
  "name": "the headline CLI stops printing the scope",
  "file": "inspeximus/cli.py",
  "old": "            for lim in res.get(\"limits\") or []:\n                print(f\"  NOTE {lim}\")\n            s = res[\"summary\"]",
  "new": "            for lim in []:\n                print(f\"  NOTE {lim}\")\n            s = res[\"summary\"]",
  "tests": [
   "tests/test_verify_bundle_content_scope.py"
  ]
 },
 {
  "name": "torch creeps back to module level (mechanism arm dies)",
  "file": "probes/agentpoison_influence_gate.py",
  "old": "import json\nimport os\nimport random\nimport sys",
  "new": "import json\nimport os\nimport random\nimport sys\nimport torch",
  "tests": [
   "tests/test_influence_gate_probes.py"
  ]
 },
 {
  "name": "the influence gate stops filtering uncorroborated memory",
  "file": "probes/agentpoison_influence_gate.py",
  "old": "        return [h for h in hits if corroborated(by_id[h[\"id\"]], by_id)]",
  "new": "        return list(hits)",
  "tests": [
   "tests/test_influence_gate_probes.py"
  ]
 },
 {
  "name": "the dense arm becomes the default again",
  "file": "probes/agentpoison_influence_gate.py",
  "old": "    want_dense = \"--dense\" in argv",
  "new": "    want_dense = _have_torch()",
  "tests": [
   "tests/test_influence_gate_probes.py"
  ]
 },
 {
  "name": "the hashing embedder stops being normalised",
  "file": "probes/agentpoison_influence_gate.py",
  "old": "    return [v / norm for v in vec] if norm else vec",
  "new": "    return vec",
  "tests": [
   "tests/test_influence_gate_probes.py"
  ]
 },
 {
  "name": "the census stops seeing module-level guards",
  "file": "tools/skip_census.py",
  "old": "        for c in (n for n in ast.walk(node) if isinstance(n, ast.Call)):",
  "new": "        for c in ():",
  "tests": [
   "tests/test_skip_census.py"
  ]
 },
 {
  "name": "the census counts by importing (environment-dependent again)",
  "file": "tools/skip_census.py",
  "old": "        absent = [d for d in deps if importlib.util.find_spec(d) is None]",
  "new": "        absent = []",
  "tests": [
   "tests/test_skip_census.py"
  ]
 },
 {
  "name": "the integrations CI job is deleted",
  "file": ".github/workflows/ci.yml",
  "old": "  integrations:",
  "new": "  integrations_disabled:",
  "tests": [
   "tests/test_skip_census.py"
  ]
 },
 {
  "name": "absence of a clash is reported as support again",
  "file": "inspeximus/core.py",
  "old": "            return _restates(text, rec_text or \"\")                          # True, or None for \"cannot tell\"",
  "new": "            return True",
  "tests": [
   "tests/test_verify_claim_undecidable.py"
  ]
 },
 {
  "name": "the restatement test accepts any overlap",
  "file": "inspeximus/core.py",
  "old": "            return True if overlap >= 0.9 else None",
  "new": "            return True",
  "tests": [
   "tests/test_verify_claim_undecidable.py"
  ]
 },
 {
  "name": "the undecidable case is folded back into 'supported'",
  "file": "inspeximus/core.py",
  "old": "            return _out(\"unverifiable\", undecidable.get(\"object\"),",
  "new": "            return _out(\"supported\", undecidable.get(\"object\"),",
  "tests": [
   "tests/test_verify_claim_undecidable.py"
  ]
 },
 {
  "name": "the keyed path stops distinguishing cannot-tell",
  "file": "inspeximus/core.py",
  "old": "            if m is None:\n                # Neither side carries a value, and the claim is not a restatement. The honest answer is\n                # that this store cannot tell -- with the record attached so the caller can judge.",
  "new": "            if False:\n                # disabled",
  "tests": [
   "tests/test_verify_claim_undecidable.py"
  ]
 },
 {
  "name": "a pruned directory goes unreported again",
  "file": "inspeximus/erasure_residue.py",
  "old": "        for d in pruned:",
  "new": "        for d in []:",
  "tests": [
   "tests/test_erasure_residue.py"
  ]
 },
 {
  "name": "skip_dirs goes back to being unioned (caller cannot opt out)",
  "file": "inspeximus/erasure_residue.py",
  "old": "    return set(_SKIP_DIRS) if skip_dirs is None else set(skip_dirs)",
  "new": "    return set(skip_dirs or ()) | _SKIP_DIRS",
  "tests": [
   "tests/test_erasure_residue.py"
  ]
 },
 {
  "name": "a nonexistent root returns clean again",
  "file": "inspeximus/erasure_residue.py",
  "old": "    if not os.path.isdir(root):",
  "new": "    if False:",
  "tests": [
   "tests/test_erasure_residue.py"
  ]
 },
 {
  "name": "zero comparisons pass again",
  "file": "inspeximus/audit_bundle.py",
  "old": "    return {\"ok\": bool(compared) and not mismatched, \"checked\": compared, \"receipted\": len(first),",
  "new": "    return {\"ok\": not mismatched, \"checked\": compared, \"receipted\": len(first),",
  "tests": [
   "tests/test_verify_bundle_content_scope.py"
  ]
 },
 {
  "name": "'checked' counts receipts instead of comparisons",
  "file": "inspeximus/audit_bundle.py",
  "old": "        compared += 1",
  "new": "        compared += 0",
  "tests": [
   "tests/test_verify_bundle_content_scope.py"
  ]
 },
 {
  "name": "the orphan truncation stops announcing itself",
  "file": "inspeximus/audit_bundle.py",
  "old": "        if len(orph) > 5:",
  "new": "        if False:",
  "tests": [
   "tests/test_verify_bundle_content_scope.py"
  ]
 },
 {
  "name": "unreceipted records go uncounted again",
  "file": "inspeximus/core.py",
  "old": "            if cur.get(\"status\") == \"active\" and mid not in committed:\n                uncommitted.append(mid)",
  "new": "            if False:\n                uncommitted.append(mid)",
  "tests": [
   "tests/test_uncounted_is_not_unchecked.py"
  ]
 },
 {
  "name": "verify_attribution stops failing closed on disabled receipts",
  "file": "inspeximus/core.py",
  "old": "        if not self.receipts_enabled and self.items:\n            # The sibling one call site over",
  "new": "        if False:\n            # The sibling one call site over",
  "tests": [
   "tests/test_uncounted_is_not_unchecked.py"
  ]
 },
 {
  "name": "coverage goes back to comparing two integers",
  "file": "inspeximus/compliance.py",
  "old": "    if require_receipts and n_receipts and uncovered:",
  "new": "    if False:",
  "tests": [
   "tests/test_uncounted_is_not_unchecked.py"
  ]
 },
 {
  "name": "autogen clear() unscopes again (erases the whole store)",
  "file": "inspeximus/integrations/autogen.py",
  "old": "               if r.get(\"status\") == \"active\" and (\n                   self._tag in (r.get(\"tags\") or [])\n                   or (self._source and (r.get(\"source\") or {}).get(\"doc\") == self._source))]",
  "new": "               if r.get(\"status\") == \"active\"]",
  "tests": [
   "tests/test_uncounted_is_not_unchecked.py"
  ]
 },
 {
  "name": "the served value leaves the commitment again",
  "file": "inspeximus/core.py",
  "old": "                \"value_sha256\": _sha256_hex(_canon(_val)),\n",
  "new": "                \"value_sha256\": \"constant\",\n",
  "tests": [
   "tests/test_value_is_committed.py"
  ]
 },
 {
  "name": "verify_writes stops comparing the value",
  "file": "inspeximus/core.py",
  "old": "                              for k in (\"immutable_sha256\", \"mtype\", \"value_sha256\",\n                                        \"status_sha256\", \"time_sha256\", \"attrib_sha256\")",
  "new": "                              for k in (\"immutable_sha256\", \"mtype\",\n                                        \"status_sha256\", \"time_sha256\", \"attrib_sha256\")",
  "tests": [
   "tests/test_value_is_committed.py"
  ]
 },
 {
  "name": "the audit bundle stops binding the value",
  "file": "inspeximus/audit_bundle.py",
  "old": "        for field in (\"immutable_sha256\", \"content_sha256\", \"value_sha256\",\n                      \"status_sha256\", \"attrib_sha256\"):",
  "new": "        for field in (\"immutable_sha256\", \"content_sha256\",\n                      \"status_sha256\", \"attrib_sha256\"):",
  "tests": [
   "tests/test_value_is_committed.py"
  ]
 },
 {
  "name": "legacy receipts are silently left uncovered",
  "file": "inspeximus/core.py",
  "old": "        if value_strict:",
  "new": "        if False:",
  "tests": [
   "tests/test_value_is_committed.py"
  ]
 },
 {
  "name": "a pre-1.82 receipt raises a false alarm on honest data",
  "file": "inspeximus/core.py",
  "old": "                              if k not in forgiven and k in rc)",
  "new": "                              if k not in forgiven)",
  "tests": [
   "tests/test_value_is_committed.py"
  ]
 },
 {
  "name": "recommit stops appending a receipt",
  "file": "inspeximus/core.py",
  "old": "            self._emit_write_receipt(rec)\n            done.append(rec[\"id\"])",
  "new": "            done.append(rec[\"id\"])",
  "tests": [
   "tests/test_value_is_committed.py"
  ]
 },
 {
  "name": "the manifest header is unbound again",
  "file": "inspeximus/deletion_manifest.py",
  "old": "        prev = _header_hash(subject, request_id, basis, authorized_by, [t.name for t in self._targets])",
  "new": "        prev = _GENESIS",
  "tests": [
   "tests/test_deletion_manifest_header.py"
  ]
 },
 {
  "name": "verify stops checking the header seed",
  "file": "inspeximus/deletion_manifest.py",
  "old": "        if first_prev == _GENESIS and first_prev != header:",
  "new": "        if False:",
  "tests": [
   "tests/test_deletion_manifest_header.py"
  ]
 },
 {
  "name": "the legacy escape hatch stops opening",
  "file": "inspeximus/deletion_manifest.py",
  "old": "        return (len(problems) == 0, problems + notes)",
  "new": "        return (len(problems + notes) == 0, problems + notes)",
  "tests": [
   "tests/test_deletion_manifest_header.py"
  ]
 },
 {
  "name": "a pinned key is ignored when verifying signatures",
  "file": "inspeximus/deletion_manifest.py",
  "old": "                if expected_pubkey is not None and e.get(\"pubkey\") != expected_pubkey:",
  "new": "                if False:",
  "tests": [
   "tests/test_deletion_manifest_header.py"
  ]
 },
 {
  "name": "the header hash drops the subject",
  "file": "inspeximus/deletion_manifest.py",
  "old": "    return _sha256({\"subject\": subject, \"request_id\": request_id, \"basis\": basis,",
  "new": "    return _sha256({\"subject\": None, \"request_id\": request_id, \"basis\": basis,",
  "tests": [
   "tests/test_deletion_manifest_header.py"
  ]
 },
 {
  "name": "zero-deps goes back to reading only installed METADATA",
  "file": "claims_audit.py",
  "old": "        pyproject = root.parent / \"pyproject.toml\"",
  "new": "        pyproject = root.parent / \"nope.toml\"",
  "tests": [
   "tests/test_audit_scripts_have_teeth.py"
  ]
 },
 {
  "name": "a skip stops counting against the gate",
  "file": "claims_audit.py",
  "old": "    return ok is None and not auditing_history",
  "new": "    return False",
  "tests": [
   "tests/test_audit_scripts_have_teeth.py"
  ]
 },
 {
  "name": "the secret sweep can be empty again",
  "file": "governance_audit.py",
  "old": "    if not declared:",
  "new": "    if False:",
  "tests": [
   "tests/test_audit_scripts_have_teeth.py"
  ]
 },
 {
  "name": "a declared secret need not appear in the scenario",
  "file": "governance_audit.py",
  "old": "    if absent:",
  "new": "    if False:",
  "tests": [
   "tests/test_audit_scripts_have_teeth.py"
  ]
 },
 {
  "name": "the audit downloads a package that does not exist",
  "file": "governance_audit.py",
  "old": "                        f\"inspeximus=={a.version}\" if a.version else \"inspeximus\",",
  "new": "                        f\"agora-inspeximus=={a.version}\" if a.version else \"agora-inspeximus\",",
  "tests": [
   "tests/test_audit_scripts_have_teeth.py"
  ]
 },
 {
  "name": "remember_decision commits the TOPIC as the value again",
  "file": "inspeximus/core.py",
  "old": "                             mtype=\"procedural\", key=key, object=(decision.strip() or None),",
  "new": "                             mtype=\"procedural\", key=key, object=(topic.strip() if topic else None),",
  "tests": [
   "tests/test_remember_decision_supersedes.py"
  ]
 },
 {
  "name": "the seeded probe drops back to five seeds",
  "file": "probes/identity_gate_supersession_probe.py",
  "old": "SEEDS = 25",
  "new": "SEEDS = 5",
  "tests": [
   "tests/test_probe_gate.py"
  ]
 },
 {
  "name": "the probe stops printing n with the range",
  "file": "probes/identity_gate_supersession_probe.py",
  "old": "          f\"sd {sd_u:.3f} +/-{rel*100:.0f}%  range {min(ung):.3f}-{max(ung):.3f} over seeds={SEEDS}  \"",
  "new": "          f\"sd {sd_u:.3f} +/-{rel*100:.0f}%  range {min(ung):.3f}-{max(ung):.3f}  \"",
  "tests": [
   "tests/test_probe_gate.py"
  ]
 },
 {
  "name": "the manipulation check stops noticing a count collapse",
  "file": "tools/probe_gate.py",
  "old": "        if len(before) != len(after):",
  "new": "        if False:",
  "tests": [
   "tests/test_probe_gate.py"
  ]
 },
 {
  "name": "the spread floor is removed",
  "file": "tools/probe_gate.py",
  "old": "        ok = len(vals) >= min_trials",
  "new": "        ok = True",
  "tests": [
   "tests/test_probe_gate.py"
  ]
 },
 {
  "name": "the harness stops restoring what it dirtied",
  "file": "tools/mutation_check.py",
  "old": "        if not _ARTIFACT.match(path.replace(\"\\\\\", \"/\")):",
  "new": "        if False:",
  "tests": [
   "tests/test_mutation_check_harness.py"
  ]
 },
 {
  "name": "examples run in the repo again (residue returns)",
  "file": "tests/test_examples_run.py",
  "old": "        cwd=tempfile.mkdtemp(prefix=\"inspeximus_example_\"), capture_output=True, text=True, encoding=\"utf-8\", errors=\"replace\", timeout=180,",
  "new": "        cwd=ROOT, capture_output=True, text=True, encoding=\"utf-8\", errors=\"replace\", timeout=180,",
  "tests": [
   "tests/test_examples_run.py"
  ]
 },
 {
  "name": "the recency tie-break is removed (crowded-store target is lost)",
  "file": "inspeximus/core.py",
  "old": "        scored.sort(key=lambda x: (-x[0], -_pos.get(x[2].get(\"id\"), -1)))",
  "new": "        scored.sort(key=lambda x: -x[0])",
  "tests": [
   "tests/test_recall_is_deterministic.py"
  ]
 },
 {
  "name": "BM25 sums its terms in set order again (float noise returns)",
  "file": "inspeximus/core.py",
  "old": "        _qterms = sorted(qtok)",
  "new": "        _qterms = list(qtok)",
  "tests": [
   "tests/test_recall_is_deterministic.py"
  ]
 },
 {
  "name": "decay age goes back to sub-second resolution",
  "file": "inspeximus/core.py",
  "old": "        age = float(int(max(0.0, now - r.get(\"last_access\", r.get(\"ts\", now)))))",
  "new": "        age = max(0.0, now - r.get(\"last_access\", r.get(\"ts\", now)))",
  "tests": [
   "tests/test_recall_is_deterministic.py"
  ]
 },
 {
  "name": "a refused revert exits 0 again",
  "file": "inspeximus/cli.py",
  "old": "        if not res.get(\"ok\"):\n            reason = res.get(\"reason\") or \"refused\"",
  "new": "        if False:\n            reason = res.get(\"reason\") or \"refused\"",
  "tests": [
   "tests/test_cli_exit_codes.py"
  ]
 },
 {
  "name": "memory_report samples the oldest 400 again",
  "file": "inspeximus/core.py",
  "old": "        sample = act if len(act) <= 400 else _random.Random(0).sample(act, 400)",
  "new": "        sample = act if len(act) <= 400 else act[:400]",
  "tests": [
   "tests/test_memory_report_sampling.py"
  ]
 },
 {
  "name": "the sample stops being seeded (evidence moves between runs)",
  "file": "inspeximus/core.py",
  "old": "else _random.Random(0).sample(act, 400)",
  "new": "else _random.sample(act, 400)",
  "tests": [
   "tests/test_memory_report_sampling.py"
  ]
 },
 {
  "name": "the harness restores source files again (eats concurrent work)",
  "file": "tools/mutation_check.py",
  "old": "        if not _ARTIFACT.match(path.replace(\"\\\\\", \"/\")):\n            continue",
  "new": "        if False:\n            continue",
  "tests": [
   "tests/test_mutation_check_harness.py"
  ]
 },
 {
  "name": "the shared store guard stops refusing a missing path",
  "file": "inspeximus/audit_bundle.py",
  "old": "    if not os.path.exists(path):\n        return None\n    return list(getattr(Inspeximus(path=path, receipts=True), \"_receipts\", None) or [])",
  "new": "    if False:\n        return None\n    return list(getattr(Inspeximus(path=path, receipts=True), \"_receipts\", None) or [])",
  "tests": [
   "tests/test_docs_examples_are_runnable.py"
  ]
 },
 {
  "name": "audit_bundle's CLI ignores the guard's refusal",
  "file": "inspeximus/cli.py",
  "old": "            receipts = load_store_receipts(a.store)\n            if items is None:",
  "new": "            receipts = load_store_receipts(a.store)\n            if False:",
  "tests": [
   "tests/test_docs_examples_are_runnable.py"
  ]
 },
 {
  "name": "the README stops pointing at the claims registry",
  "file": "README.md",
  "old": "**Every number on this page is registered in [docs/CLAIMS.md](docs/CLAIMS.md)**",
  "new": "**Every number on this page traces to a runnable probe.**",
  "tests": [
   "tests/test_claims_coverage.py"
  ]
 },
 {
  "name": "the surface opener stops setting the echo-guard posture (the wedge returns)",
  "file": "inspeximus/_surface.py",
  "old": "    store.echo_guard = echo_guard_default()",
  "new": "    store.echo_guard = False",
  "tests": [
   "tests/test_surface_parity.py"
  ]
 },
 {
  "name": "the surface default flips to OFF unless asked",
  "file": "inspeximus/core.py",
  "old": "    return os.environ.get(\"INSPEXIMUS_ECHO_GUARD\", \"1\") != \"0\"",
  "new": "    return os.environ.get(\"INSPEXIMUS_ECHO_GUARD\", \"0\") != \"0\"",
  "tests": [
   "tests/test_surface_parity.py",
   "tests/test_echo_guard_posture.py"
  ]
 },
 {
  "name": "the surface stops keeping an existing receipt chain (a hole in the evidence)",
  "file": "inspeximus/_surface.py",
  "old": "    if not receipts and p and os.path.exists(str(p) + \".receipts.json\"):\n        receipts = True",
  "new": "    if False:\n        receipts = True",
  "tests": [
   "tests/test_surface_parity.py"
  ]
 },
 {
  "name": "the surface resolves a path an adapter meant to keep in memory",
  "file": "inspeximus/_surface.py",
  "old": "    p = resolve_path(path) if resolve else path",
  "new": "    p = resolve_path(path)",
  "tests": [
   "tests/test_surface_parity.py"
  ]
 },
 {
  "name": "an adapter goes back to constructing the store itself",
  "file": "inspeximus/integrations/langchain.py",
  "old": "        self.store = store if store is not None else open_store(path, embed=embed, resolve=False)",
  "new": "        self.store = store if store is not None else Inspeximus(path=path, embed=embed)",
  "tests": [
   "tests/test_surface_parity.py"
  ]
 },
 {
  "name": "the editor hook goes back to constructing the store itself",
  "file": "inspeximus/claude_code.py",
  "old": "    return open_store(os.path.join(d, \"coding_memory.json\"), embed=emb_doc, embed_query=emb_query,",
  "new": "    return Inspeximus(os.path.join(d, \"coding_memory.json\"), embed=emb_doc, embed_query=emb_query,",
  "tests": [
   "tests/test_surface_parity.py"
  ]
 },
 {
  "name": "the MCP server goes back to constructing the store itself",
  "file": "inspeximus/mcp_server.py",
  "old": "_MEM = open_store(_PATH, embed=_EMB_DOC",
  "new": "_MEM = Inspeximus(_PATH, embed=_EMB_DOC",
  "tests": [
   "tests/test_surface_parity.py"
  ]
 },
 {
  "name": "MCP verify_audit_bundle drops store_items again (content never compared)",
  "file": "inspeximus/mcp_server.py",
  "old": "    return verify_bundle(bundle, witnesses=witnesses, threshold=threshold, store_items=items,\n                         store_receipts=receipts, require_signed=require_signed,\n                         expected_pubkey=(expected_pubkey or None))",
  "new": "    return verify_bundle(bundle, witnesses=witnesses, threshold=threshold,\n                         store_receipts=receipts, require_signed=require_signed,\n                         expected_pubkey=(expected_pubkey or None))",
  "tests": [
   "tests/test_mcp_verification_gaps.py"
  ]
 },
 {
  "name": "MCP verify_audit_bundle downgrades a missing store_path instead of refusing",
  "file": "inspeximus/mcp_server.py",
  "old": "        if items is None:",
  "new": "        if False:",
  "tests": [
   "tests/test_mcp_verification_gaps.py"
  ]
 },
 {
  "name": "a skipped mutation counts as a pass again (unevaluated reads as clean)",
  "file": "tools/mutation_check.py",
  "old": "    return 1 if (survived or skipped) else 0",
  "new": "    return 1 if survived else 0",
  "tests": [
   "tests/test_mutation_check_harness.py"
  ]
 },
 {
  "name": "the keep-budget goes back to slicing a population an earlier pass already retired",
  "file": "inspeximus/core.py",
  "old": "        active = [r for r in active if r.get(\"status\") == \"active\" and not _is_guard_record(r)]\n        if keep is not None and len(active) > keep:",
  "new": "        if keep is not None and len(active) > keep:",
  "tests": [
   "tests/test_consolidate_keep_budget.py"
  ]
 },
 {
  "name": "the keep-budget demotes guard bookkeeping again, silencing check_code",
  "file": "inspeximus/core.py",
  "old": "        active = [r for r in active if r.get(\"status\") == \"active\" and not _is_guard_record(r)]\n        if keep is not None and len(active) > keep:",
  "new": "        active = [r for r in active if r.get(\"status\") == \"active\"]\n        if keep is not None and len(active) > keep:",
  "tests": [
   "tests/test_guard_keyspace_survives_housekeeping.py"
  ]
 },
 {
  "name": "capacity eviction hard-deletes deprecation records again",
  "file": "inspeximus/core.py",
  "old": "        active = [r for r in self.items if r.get(\"status\") == \"active\" and not _is_guard_record(r)]\n        if len(active) <= self.capacity:",
  "new": "        active = [r for r in self.items if r.get(\"status\") == \"active\"]\n        if len(active) <= self.capacity:",
  "tests": [
   "tests/test_guard_keyspace_survives_housekeeping.py"
  ]
 },
 {
  "name": "the guard keyspace registry goes empty, so nothing is carved out",
  "file": "inspeximus/core.py",
  "old": "_GUARD_KEYSPACES = (_CODE_GUARD_PREFIX, _ACL_PREFIX)",
  "new": "_GUARD_KEYSPACES = ()",
  "tests": [
   "tests/test_guard_keyspace_survives_housekeeping.py"
  ]
 },
 {
  "name": "check_code stops matching the tail, missing qualified refactor records again",
  "file": "inspeximus/code_guard.py",
  "old": "    if tail and subject and re.search(_ident(subject), code):\n        return _ident(tail)",
  "new": "    if False:\n        return _ident(tail)",
  "tests": [
   "tests/test_code_guard_qualified_symbols.py"
  ]
 },
 {
  "name": "check_code matches the tail WITHOUT the subject check, flagging every unrelated object",
  "file": "inspeximus/code_guard.py",
  "old": "    if tail and subject and re.search(_ident(subject), code):\n        return _ident(tail)",
  "new": "    if tail:\n        return _ident(tail)",
  "tests": [
   "tests/test_code_guard_qualified_symbols.py"
  ]
 },
 {
  "name": "consolidate reports the keep REQUEST as if it were the survivors again",
  "file": "inspeximus/core.py",
  "old": "\"staled\": staled, \"kept\": _live, \"keep_requested\": keep,",
  "new": "\"staled\": staled, \"kept\": keep, \"keep_requested\": keep,",
  "tests": [
   "tests/test_consolidate_keep_budget.py"
  ]
 },
 {
  "name": "an unsigned certificate claims its signatures are valid again",
  "file": "inspeximus/core.py",
  "old": "    signed = [t for t in toms if t.get(\"sig\")]",
  "new": "    signed = list(toms)",
  "tests": [
   "tests/test_certificate_scope_and_signing.py"
  ]
 },
 {
  "name": "the certificate's scope statement goes back to being unchecked free text",
  "file": "inspeximus/core.py",
  "old": "    if _scope_txt is not None and _scope_txt != _CERT_SCOPE:",
  "new": "    if False:",
  "tests": [
   "tests/test_certificate_scope_and_signing.py"
  ]
 },
 {
  "name": "publishing stops depending on the test suite (a red suite reaches PyPI again)",
  "file": ".github/workflows/release.yml",
  "old": "    needs: [audit, tests]",
  "new": "    needs: audit",
  "tests": [
   "tests/test_release_cannot_publish_untested.py"
  ]
 },
 {
  "name": "the release pinner goes back to leaving the plugin manifests behind",
  "file": "packages/_pin_server_json.py",
  "old": "    p = ROOT / \".claude-plugin\" / \"plugin.json\"\n    if p.exists():",
  "new": "    p = ROOT / \".claude-plugin\" / \"plugin.json\"\n    if False:",
  "tests": [
   "tests/test_manifest_versions.py"
  ]
 },
 {
  "name": "the pinner overwrites the marketplace SCHEMA version with the package version",
  "file": "packages/_pin_server_json.py",
  "old": "        for entry in d.get(\"plugins\", []):        # NOT d[\"version\"] -- that is the schema's own",
  "new": "        d[\"version\"] = version\n        for entry in d.get(\"plugins\", []):",
  "tests": [
   "tests/test_manifest_versions.py"
  ]
 },
 {
  "name": "a mutant's falsified receipt is left in the tree for the rest of the run",
  "file": "tools/mutation_check.py",
  "old": "            for _p in _restore(_dirty_tracked() - dirty_before):\n                restored_all.add(_p)",
  "new": "            pass",
  "tests": [
   "tests/test_mutation_check_harness.py"
  ]
 },
 {
  "name": "the receipt allowlist goes back to a suffix a fifth of the probes ignore",
  "file": "tools/mutation_check.py",
  "old": "_ARTIFACT = re.compile(r\"^probes/[\\w.-]+\\.json$\")",
  "new": "_ARTIFACT = re.compile(r\"^probes/[\\w.-]+_result\\.json$\")",
  "tests": [
   "tests/test_mutation_check_harness.py"
  ]
 },
 {
  "name": "MCP compliance_check drops prior_anchor again (not_append_only cannot fire)",
  "file": "inspeximus/mcp_server.py",
  "old": "               prior_anchor=prior_anchor, expected_pubkey=_pin(expected_pubkey))",
  "new": "               prior_anchor=None, expected_pubkey=_pin(expected_pubkey))",
  "tests": [
   "tests/test_mcp_verification_gaps.py"
  ]
 },
 {
  "name": "subject matching goes back to the coarse host-only key (ghost DSAR deletes a stranger)",
  "file": "inspeximus/core.py",
  "old": "        ids = self._narrow_to_subject(subject, ids)",
  "new": "        ids = list(ids)",
  "tests": [
   "tests/test_isolation_and_persistence.py"
  ]
 },
 {
  "name": "narrowing drops the derived-lineage cascade again (erasure becomes INCOMPLETE)",
  "file": "inspeximus/core.py",
  "old": "        keep |= inherited\n        return [rid for rid in ids if rid in keep]",
  "new": "        return [rid for rid in ids if rid in roots]",
  "tests": [
   "tests/test_erasure_preview.py",
   "tests/test_erasure_audit.py"
  ]
 },
 {
  "name": "forget_pii re-matches on the coarse candidate set, bypassing the resolver",
  "file": "inspeximus/core.py",
  "old": "            if sel_ids is not None and r[\"id\"] not in sel_ids:\n                continue",
  "new": "            if False:\n                continue",
  "tests": [
   "tests/test_isolation_and_persistence.py"
  ]
 },
 {
  "name": "erasure coverage reports complete without reading the manifest",
  "file": "inspeximus/core.py",
  "old": "        cov[\"complete\"] = bool(manifest.get(\"complete\"))",
  "new": "        cov[\"complete\"] = True",
  "tests": [
   "tests/test_erasure_coverage.py"
  ]
 },
 {
  "name": "the write verdict is never reset, so a stale one is reported as this call's",
  "file": "inspeximus/core.py",
  "old": "        self.last_write = {\"id\": mid, \"key\": key, \"status\": \"active\", \"blocked\": False, \"policy\": None}",
  "new": "        self.last_write = getattr(self, \"last_write\", None) or {\"id\": mid, \"key\": key, \"status\": \"active\", \"blocked\": False, \"policy\": None}",
  "tests": [
   "tests/test_write_verdict.py"
  ]
 },
 {
  "name": "every write reports blocked, so the verdict carries no information",
  "file": "inspeximus/core.py",
  "old": "        self.last_write = {\"id\": mid, \"key\": key, \"status\": \"active\", \"blocked\": False, \"policy\": None}",
  "new": "        self.last_write = {\"id\": mid, \"key\": key, \"status\": \"active\", \"blocked\": True, \"policy\": None}",
  "tests": [
   "tests/test_write_verdict.py"
  ]
 },
 {
  "name": "the echo-guard resolver ignores an explicit argument in favour of the environment",
  "file": "inspeximus/core.py",
  "old": "    if explicit is not None:\n        return bool(explicit)\n    return os.environ.get(\"INSPEXIMUS_ECHO_GUARD\", \"1\") != \"0\"",
  "new": "    return os.environ.get(\"INSPEXIMUS_ECHO_GUARD\", \"1\") != \"0\"",
  "tests": [
   "tests/test_echo_guard_posture.py"
  ]
 },
 {
  "name": "the echo-guard resolver ignores the environment, reviving the dead off-switch",
  "file": "inspeximus/core.py",
  "old": "    return os.environ.get(\"INSPEXIMUS_ECHO_GUARD\", \"1\") != \"0\"",
  "new": "    return True",
  "tests": [
   "tests/test_echo_guard_posture.py"
  ]
 },
 {
  "name": "the overview counts ordinary supersessions as writes retired on arrival",
  "file": "inspeximus/core.py",
  "old": "        retired = {p: n for p, n in by_policy.items() if p in (\"echo_guard\", \"objectless_guard\")}",
  "new": "        retired = dict(by_policy)",
  "tests": [
   "tests/test_report_shows_retired_writes.py"
  ]
 },
 {
  "name": "the overview always reports nothing was retired",
  "file": "inspeximus/core.py",
  "old": "        retired = {p: n for p, n in by_policy.items() if p in (\"echo_guard\", \"objectless_guard\")}",
  "new": "        retired = {}",
  "tests": [
   "tests/test_report_shows_retired_writes.py"
  ]
 },
 {
  "name": "the erasure audit selects by the coarse key again",
  "file": "inspeximus/core.py",
  "old": "            for rid in self._narrow_to_subject(subject, coarse):",
  "new": "            for rid in coarse:",
  "tests": [
   "tests/test_erasure_audit_subject.py"
  ]
 },
 {
  "name": "the erasure audit narrows to nothing, so it can never report residue",
  "file": "inspeximus/core.py",
  "old": "            for rid in self._narrow_to_subject(subject, coarse):",
  "new": "            for rid in []:",
  "tests": [
   "tests/test_erasure_audit_subject.py"
  ]
 },
 {
  "name": "the erasure preview claims completeness with nothing contacted",
  "file": "inspeximus/core.py",
  "old": "               \"coverage\": dict(Inspeximus._erasure_coverage(\n                   None, len(getattr(self, \"_erasure_targets\", []))), preview=True),",
  "new": "               \"coverage\": {\"complete\": True, \"preview\": True, \"unregistered\": False, \"external_targets\": 0, \"note\": \"\"},",
  "tests": [
   "tests/test_erasure_audit_subject.py"
  ]
 },
 {
  "name": "the erasure preview is not labelled as a rehearsal",
  "file": "inspeximus/core.py",
  "old": "                   None, len(getattr(self, \"_erasure_targets\", []))), preview=True),",
  "new": "                   None, len(getattr(self, \"_erasure_targets\", []))), preview=False),",
  "tests": [
   "tests/test_erasure_audit_subject.py"
  ]
 },
 {
  "name": "confirmed counts the store's own attestation again",
  "file": "inspeximus/core.py",
  "old": "        ext = [e for e in entries if e.get(\"target\") != \"inspeximus-store\"]",
  "new": "        ext = list(entries)",
  "tests": [
   "tests/test_coverage_confirmed_counts_externals.py"
  ]
 },
 {
  "name": "confirmed can never be non-zero, so the field is not a measurement",
  "file": "inspeximus/core.py",
  "old": "        cov[\"confirmed\"] = sum(1 for e in ext if e.get(\"verified_absent\") is True)",
  "new": "        cov[\"confirmed\"] = 0",
  "tests": [
   "tests/test_coverage_confirmed_counts_externals.py"
  ]
 },
 {
  "name": "a dissenting target is counted as a confirmation",
  "file": "inspeximus/core.py",
  "old": "        cov[\"confirmed\"] = sum(1 for e in ext if e.get(\"verified_absent\") is True)",
  "new": "        cov[\"confirmed\"] = len(ext)",
  "tests": [
   "tests/test_coverage_confirmed_counts_externals.py"
  ]
 },
 {
  "name": "the store's own self-check is dropped from the report",
  "file": "inspeximus/core.py",
  "old": "        cov[\"store_self_check\"] = next(\n            (bool(e.get(\"verified_absent\")) for e in entries if e.get(\"target\") == \"inspeximus-store\"),\n            None)",
  "new": "        cov[\"store_self_check\"] = None",
  "tests": [
   "tests/test_coverage_confirmed_counts_externals.py"
  ]
 },
 {
  "name": "the gate restores through a newline-translating writer",
  "file": "tools/mutation_check.py",
  "old": "    with io.open(path, \"w\", encoding=\"utf-8\", newline=\"\") as fh:\n        fh.write(text)",
  "new": "    with io.open(path, \"w\", encoding=\"utf-8\") as fh:\n        fh.write(text)",
  "tests": [
   "tests/test_mutation_restore_is_byte_exact.py"
  ]
 },
 {
  "name": "the gate reads through a newline-translating reader",
  "file": "tools/mutation_check.py",
  "old": "    return io.open(path, encoding=\"utf-8\", newline=\"\").read()",
  "new": "    return io.open(path, encoding=\"utf-8\").read()",
  "tests": [
   "tests/test_mutation_restore_is_byte_exact.py"
  ]
 },
 {
  "name": "the gate stops matching spec endings to the file",
  "file": "tools/mutation_check.py",
  "old": "    if \"\\r\\n\" in src and \"\\r\\n\" not in fragment:\n        return fragment.replace(\"\\n\", \"\\r\\n\")\n    return fragment",
  "new": "    return fragment",
  "tests": [
   "tests/test_mutation_restore_is_byte_exact.py"
  ]
 },
 {
  "name": "the gate converts endings unconditionally, breaking LF files",
  "file": "tools/mutation_check.py",
  "old": "    if \"\\r\\n\" in src and \"\\r\\n\" not in fragment:\n        return fragment.replace(\"\\n\", \"\\r\\n\")\n    return fragment",
  "new": "    return fragment.replace(\"\\n\", \"\\r\\n\")",
  "tests": [
   "tests/test_mutation_restore_is_byte_exact.py"
  ]
 },
 {
  "name": "the MCP server drops the source it was given",
  "file": "inspeximus/mcp_server.py",
  "old": "                        source={\"doc\": source} if source else None,",
  "new": "                        source=None,",
  "tests": [
   "tests/test_mcp_provenance_reach.py"
  ]
 },
 {
  "name": "the MCP server passes the source in a shape the resolver cannot read",
  "file": "inspeximus/mcp_server.py",
  "old": "                        source={\"doc\": source} if source else None,",
  "new": "                        source={\"name\": source} if source else None,",
  "tests": [
   "tests/test_mcp_provenance_reach.py"
  ]
 },
 {
  "name": "the MCP server invents a subject the caller never supplied",
  "file": "inspeximus/mcp_server.py",
  "old": "                        source={\"doc\": source} if source else None,",
  "new": "                        source={\"doc\": source or \"hr/bob\"},",
  "tests": [
   "tests/test_mcp_provenance_reach.py"
  ]
 },
 {
  "name": "the MCP server drops the declared lineage",
  "file": "inspeximus/mcp_server.py",
  "old": "                        derived_from=derived_from or None,",
  "new": "                        derived_from=None,",
  "tests": [
   "tests/test_mcp_provenance_reach.py"
  ]
 },
 {
  "name": "the write result drops the declared lineage it reports back",
  "file": "inspeximus/mcp_server.py",
  "old": "            \"mtype\": rec.get(\"mtype\"), \"source\": source or None,\n            \"derived_from\": list(derived_from or []),",
  "new": "            \"mtype\": rec.get(\"mtype\"), \"source\": source or None,\n            \"derived_from\": [],",
  "tests": [
   "tests/test_mcp_provenance_reach.py"
  ]
 },
 {
  "name": "the CLI drops the declared lineage",
  "file": "inspeximus/cli.py",
  "old": "                         derived_from=a.derived_from or None)",
  "new": "                         derived_from=None)",
  "tests": [
   "tests/test_cli_provenance.py"
  ]
 },
 {
  "name": "the decision path drops the source it was given",
  "file": "inspeximus/core.py",
  "old": "                             source={\"doc\": source} if isinstance(source, str) and source else source,\n                             derived_from=derived_from or None,\n                             user_id=user_id, agent_id=agent_id, session_id=session_id)",
  "new": "                             source=None,\n                             derived_from=derived_from or None,\n                             user_id=user_id, agent_id=agent_id, session_id=session_id)",
  "tests": [
   "tests/test_write_paths_provenance_census.py"
  ]
 },
 {
  "name": "the decision path invents a subject the caller never supplied",
  "file": "inspeximus/core.py",
  "old": "                             source={\"doc\": source} if isinstance(source, str) and source else source,\n                             derived_from=derived_from or None,\n                             user_id=user_id, agent_id=agent_id, session_id=session_id)",
  "new": "                             source={\"doc\": source or \"hr/bob\"},\n                             derived_from=derived_from or None,\n                             user_id=user_id, agent_id=agent_id, session_id=session_id)",
  "tests": [
   "tests/test_write_paths_provenance_census.py"
  ]
 },
 {
  "name": "the decision result always claims the record is attributable",
  "file": "inspeximus/mcp_server.py",
  "old": "            \"supersedes_by_key\": bool(topic), \"project\": _PROJECT,\n            \"attributable\": bool(source) or bool(derived_from),",
  "new": "            \"supersedes_by_key\": bool(topic), \"project\": _PROJECT,\n            \"attributable\": True,",
  "tests": [
   "tests/test_write_paths_provenance_census.py"
  ]
 },
 {
  "name": "the CLI decision path drops the source it was given",
  "file": "inspeximus/cli.py",
  "old": "                                  source=a.source or None, derived_from=a.derived_from or None)",
  "new": "                                  source=None, derived_from=None)",
  "tests": [
   "tests/test_cli_provenance.py"
  ]
 },
 {
  "name": "the CLI decision path invents a subject the caller never supplied",
  "file": "inspeximus/cli.py",
  "old": "                                  source=a.source or None, derived_from=a.derived_from or None)",
  "new": "                                  source=a.source or \"hr/bob\", derived_from=a.derived_from or None)",
  "tests": [
   "tests/test_cli_provenance.py"
  ]
 },
 {
  "name": "the census reads a guard out of prose again",
  "file": "tools/skip_census.py",
  "old": "            if name != \"importorskip\" or not c.args:",
  "new": "            if False:",
  "tests": [
   "tests/test_skip_census.py"
  ]
 },
 {
  "name": "the census stops detecting real guards",
  "file": "tools/skip_census.py",
  "old": "                deps.append(arg.value)\n                found_guard = True",
  "new": "                found_guard = True",
  "tests": [
   "tests/test_skip_census.py"
  ]
 },
 {
  "name": "the erasure stops checking its own store for residue",
  "file": "inspeximus/core.py",
  "old": "        out[\"residue_in_store\"] = scan_records(self.items, _residue_values)",
  "new": "        out[\"residue_in_store\"] = {\"ok\": True, \"findings\": [], \"problems\": [], \"checked_records\": 0, \"searched_values\": 0, \"method\": \"\"}",
  "tests": [
   "tests/test_instore_residue.py"
  ]
 },
 {
  "name": "the residue report echoes the erased value instead of a fingerprint",
  "file": "inspeximus/erasure_residue.py",
  "old": "                    findings.append({\"id\": r.get(\"id\"), \"field\": field,\n                                     \"fingerprint\": _fingerprint(v)})",
  "new": "                    findings.append({\"id\": r.get(\"id\"), \"field\": field,\n                                     \"fingerprint\": v})",
  "tests": [
   "tests/test_instore_residue.py"
  ]
 },
 {
  "name": "an empty in-store search reports clean",
  "file": "inspeximus/erasure_residue.py",
  "old": "        return {\"ok\": False, \"checked_records\": len(recs), \"searched_values\": 0, \"findings\": [],",
  "new": "        return {\"ok\": True, \"checked_records\": len(recs), \"searched_values\": 0, \"findings\": [],",
  "tests": [
   "tests/test_instore_residue.py"
  ]
 },
 {
  "name": "short values are searched, so residue matches everywhere",
  "file": "inspeximus/erasure_residue.py",
  "old": "    vals = [v for v in {str(v).strip() for v in (values or [])} if len(v) >= 4]",
  "new": "    vals = [v for v in {str(v).strip() for v in (values or [])} if len(v) >= 1]",
  "tests": [
   "tests/test_instore_residue.py"
  ]
 },
 {
  "name": "a bounded in-store scan does not say it was bounded",
  "file": "inspeximus/erasure_residue.py",
  "old": "            problems.append(f\"stopped after {checked} record(s): the comparison budget was reached, so \"\n                            f\"{len(recs) - checked} record(s) were NOT examined\")",
  "new": "            pass",
  "tests": [
   "tests/test_instore_residue.py"
  ]
 },
 {
  "name": "forget_subject stops carrying the residue report",
  "file": "inspeximus/core.py",
  "old": "               \"residue_in_store\": res.get(\"residue_in_store\")}\n        if targets:",
  "new": "               \"residue_in_store\": None}\n        if targets:",
  "tests": [
   "tests/test_instore_residue.py"
  ]
 },
 {
  "name": "forget_pii stops carrying the residue report",
  "file": "inspeximus/core.py",
  "old": "                \"coverage\": res.get(\"coverage\", cov),\n                \"unswept_matches\": unswept_block,\n                \"residue_in_store\": res.get(\"residue_in_store\")}",
  "new": "                \"coverage\": res.get(\"coverage\", cov),\n                \"unswept_matches\": unswept_block,\n                \"residue_in_store\": None}",
  "tests": [
   "tests/test_instore_residue.py"
  ]
 },
 {
  "name": "route stops declaring a parent at every write site",
  "file": "inspeximus/core.py",
  "old": "            cur_rec = self._current_active(kk) if kk else None\n            return [cur_rec[\"id\"]] if cur_rec else None",
  "new": "            return None",
  "tests": [
   "tests/test_route_correction_lineage.py"
  ]
 },
 {
  "name": "route declares a parent even when there is no key",
  "file": "inspeximus/core.py",
  "old": "            cur_rec = self._current_active(kk) if kk else None",
  "new": "            cur_rec = self._current_active(kk) if kk else (self.items[0] if self.items else None)",
  "tests": [
   "tests/test_route_correction_lineage.py"
  ]
 },
 {
  "name": "route drops the source at every write site",
  "file": "inspeximus/core.py",
  "old": "        _src = {\"doc\": source} if isinstance(source, str) and source else source",
  "new": "        _src = None",
  "tests": [
   "tests/test_route_correction_lineage.py"
  ]
 },
 {
  "name": "the echo branch writes without provenance again",
  "file": "inspeximus/core.py",
  "old": "            rid = self.remember(text, key=key, object=object,      # guard retires it, judge-logged\n                                source=_src, derived_from=_parent(), project=project)",
  "new": "            rid = self.remember(text, key=key, object=object, project=project)",
  "tests": [
   "tests/test_route_correction_lineage.py"
  ]
 },
 {
  "name": "the keyless echo branch writes without provenance again",
  "file": "inspeximus/core.py",
  "old": "            rid = self.remember(text, meta={\"routed\": \"echo_unkeyed\"},\n                                source=_src, derived_from=_parent(), project=project)",
  "new": "            rid = self.remember(text, meta={\"routed\": \"echo_unkeyed\"}, project=project)",
  "tests": [
   "tests/test_route_correction_lineage.py"
  ]
 },
 {
  "name": "the keyless echo branch gains a key and can clobber",
  "file": "inspeximus/core.py",
  "old": "            rid = self.remember(text, meta={\"routed\": \"echo_unkeyed\"},\n                                source=_src, derived_from=_parent(), project=project)",
  "new": "            rid = self.remember(text, key=key, object=object, meta={\"routed\": \"echo_unkeyed\"},\n                                source=_src, derived_from=_parent(), project=project)",
  "tests": [
   "tests/test_route_correction_lineage.py"
  ]
 },
 {
  "name": "_parent ignores the resolved key and reads the parameter",
  "file": "inspeximus/core.py",
  "old": "            kk = k if k is not None else key",
  "new": "            kk = key",
  "tests": [
   "tests/test_route_correction_lineage.py"
  ]
 },
 {
  "name": "the revert-restore branch stops declaring a parent",
  "file": "inspeximus/core.py",
  "old": "                                    capability=capability, meta={\"routed\": \"revert_named\"},\n                                    source=_src, derived_from=_parent(k), project=project)",
  "new": "                                    capability=capability, meta={\"routed\": \"revert_named\"}, project=project)",
  "tests": [
   "tests/test_route_correction_lineage.py"
  ]
 },
 {
  "name": "the revert-to-original branch stops declaring a parent",
  "file": "inspeximus/core.py",
  "old": "                                    capability=capability, meta={\"routed\": \"revert_original\"},\n                                    source=_src, derived_from=_parent(k), project=project)",
  "new": "                                    capability=capability, meta={\"routed\": \"revert_original\"}, project=project)",
  "tests": [
   "tests/test_route_correction_lineage.py"
  ]
 },
 {
  "name": "the keyless delete branch drops the caller's source",
  "file": "inspeximus/core.py",
  "old": "                rid = self.remember(text, source=_src, project=project)\n                return {\"intent\": \"delete\"",
  "new": "                rid = self.remember(text, project=project)\n                return {\"intent\": \"delete\"",
  "tests": [
   "tests/test_route_correction_lineage.py"
  ]
 },
 {
  "name": "the keyless revert branch drops the caller's source",
  "file": "inspeximus/core.py",
  "old": "                rid = self.remember(text, source=_src, project=project)      # same as the delete branch",
  "new": "                rid = self.remember(text, project=project)      # same as the delete branch",
  "tests": [
   "tests/test_route_correction_lineage.py"
  ]
 },
 {
  "name": "distill swallows a failed write again",
  "file": "inspeximus/core.py",
  "old": "                errors.append(f\"{type(e).__name__}: {str(e)[:120]}\")",
  "new": "                pass",
  "tests": [
   "tests/test_distill_reports_failures.py"
  ]
 },
 {
  "name": "distill reports an error block on a clean run",
  "file": "inspeximus/core.py",
  "old": "        if errors:\n            out[\"errors\"] = errors\n            out[\"failed\"] = len(errors)",
  "new": "        out[\"errors\"] = errors\n        out[\"failed\"] = len(errors)",
  "tests": [
   "tests/test_distill_reports_failures.py"
  ]
 },
 {
  "name": "distill counts every failure as one",
  "file": "inspeximus/core.py",
  "old": "            out[\"failed\"] = len(errors)",
  "new": "            out[\"failed\"] = 1",
  "tests": [
   "tests/test_distill_reports_failures.py"
  ]
 },
 {
  "name": "the CLI distill passes a bare string source",
  "file": "inspeximus/cli.py",
  "old": "                                     source={\"doc\": a.source} if a.source else None)",
  "new": "                                     source=a.source or None)",
  "tests": [
   "tests/test_distill_reports_failures.py",
   "tests/test_cli_provenance.py"
  ]
 },
 {
  "name": "the routed delete drops the erasure fields again",
  "file": "inspeximus/core.py",
  "old": "                    \"forgotten\": res.get(\"forgotten\", 0),\n                    \"coverage\": res.get(\"coverage\"),\n                    \"residue_in_store\": res.get(\"residue_in_store\")}",
  "new": "                    \"forgotten\": res.get(\"forgotten\", 0)}",
  "tests": [
   "tests/test_route_delete_answers_like_an_erasure.py"
  ]
 },
 {
  "name": "the empty forget early return loses coverage again",
  "file": "inspeximus/core.py",
  "old": "            return {\"forgotten\": 0, \"ids\": [], \"scrubbed_links\": 0, \"tombstones\": 0,\n                    \"coverage\": self._erasure_coverage(None, len(getattr(self, \"_erasure_targets\", []))),",
  "new": "            return {\"forgotten\": 0, \"ids\": [], \"scrubbed_links\": 0, \"tombstones\": 0,",
  "tests": [
   "tests/test_route_delete_answers_like_an_erasure.py"
  ]
 },
 {
  "name": "the spread is reported without the c4 bias correction",
  "file": "tools/probe_gate.py",
  "old": "        sd_hat = (statistics.stdev(vals) / c4(n)) if n >= 2 else 0.0",
  "new": "        sd_hat = (statistics.stdev(vals)) if n >= 2 else 0.0",
  "tests": [
   "tests/test_probe_gate.py"
  ]
 },
 {
  "name": "the corrected SD stops stating its own uncertainty",
  "file": "tools/probe_gate.py",
  "old": "    rel = math.sqrt(max(0.0, 1.0 - c * c)) / c",
  "new": "    rel = math.sqrt(max(0.0, 1.0 - c * c))",
  "tests": [
   "tests/test_probe_gate.py"
  ]
 },
 {
  "name": "the probe artifact divides by a stale seed count again",
  "file": "probes/identity_gate_supersession_probe.py",
  "old": "    cand_per_run = ncand / SEEDS",
  "new": "    cand_per_run = ncand / 5",
  "tests": [
   "tests/test_probe_gate.py"
  ]
 },
 {
  "name": "the kurtosis inflation is dropped from sd_rel_error",
  "file": "tools/probe_gate.py",
  "old": "        rel *= max(1.0, math.sqrt(max(0.0, 1.0 + g2 / 2.0)))",
  "new": "        rel *= 1.0",
  "tests": [
   "tests/test_probe_gate.py"
  ]
 },
 {
  "name": "the inflation loses its floor and may SHRINK the stated uncertainty",
  "file": "tools/probe_gate.py",
  "old": "        rel *= max(1.0, math.sqrt(max(0.0, 1.0 + g2 / 2.0)))",
  "new": "        rel *= math.sqrt(max(0.0, 1.0 + g2 / 2.0))",
  "tests": [
   "tests/test_probe_gate.py"
  ]
 },
 {
  "name": "spread() stops passing its values, so the +/- reverts to pure normal theory",
  "file": "tools/probe_gate.py",
  "old": "        rel = sd_rel_error(n, vals if n >= 4 else None)",
  "new": "        rel = sd_rel_error(n)",
  "tests": [
   "tests/test_probe_gate.py"
  ]
 },
 {
  "name": "the recall window is never stamped onto the write",
  "file": "inspeximus/core.py",
  "old": "            rec[\"recall_window\"] = _obs",
  "new": "            pass",
  "tests": [
   "tests/test_recall_window_observation.py"
  ]
 },
 {
  "name": "forget() scrubs the recall window, deleting the erasure evidence",
  "file": "inspeximus/core.py",
  "old": "        scrubbed = 0\n        if redact_links:",
  "new": "        scrubbed = 0\n        for _r in self.items:\n            _rw = _r.get(\"recall_window\")\n            if _rw: _rw[\"ids\"] = [i for i in _rw[\"ids\"] if i not in target]\n        if redact_links:",
  "tests": [
   "tests/test_recall_window_observation.py"
  ]
 },
 {
  "name": "erasure_audit goes blind to the recall_window pointer channel",
  "file": "inspeximus/core.py",
  "old": "            for pid in [p for p in ((r.get(\"recall_window\") or {}).get(\"ids\") or []) if p not in by_id]:",
  "new": "            for pid in []:",
  "tests": [
   "tests/test_recall_window_observation.py"
  ]
 },
 {
  "name": "a capped window truncates silently, hiding its true width",
  "file": "inspeximus/core.py",
  "old": "                _obs[\"n\"] = len(_win)",
  "new": "                pass",
  "tests": [
   "tests/test_recall_window_observation.py"
  ]
 },
 {
  "name": "the writes-since-recall counter never resets",
  "file": "inspeximus/core.py",
  "old": "        self._last_recall_writes = 0\n",
  "new": "        pass\n",
  "tests": [
   "tests/test_recall_window_observation.py"
  ]
 },
 {
  "name": "an iterative recall stamps only its last hop, not the union",
  "file": "inspeximus/core.py",
  "old": "        self._note_recall_window(list(seen), query)\n",
  "new": "",
  "tests": [
   "tests/test_recall_window_observation.py"
  ]
 },
 {
  "name": "the two-phase surface drops round 1 from the window",
  "file": "inspeximus/core.py",
  "old": "        self._note_recall_window(merged, query)\n",
  "new": "",
  "tests": [
   "tests/test_recall_window_observation.py"
  ]
 },
 {
  "name": "a foreign (observe=False) read is observed anyway",
  "file": "inspeximus/core.py",
  "old": "                self._last_recall_at = 0.0      # sentinel",
  "new": "                self._last_recall_at = self._last_recall_at or 0.0  # sentinel",
  "tests": [
   "tests/test_recall_window_observation.py"
  ]
 },
 {
  "name": "the MCP server reads the observe flag and never passes it to the store",
  "file": "inspeximus/mcp_server.py",
  "old": "_MEM = open_store(_PATH, embed=_EMB_DOC, embed_query=_EMB_QUERY, embed_id=_EMB_ID, receipts=_RECEIPTS,\n                  receipt_key=_SIGNING[\"key\"], observe_recall=_OBSERVE_RECALL, writer_key=_WRITER_KEY,\n                  persist_vectors=_PERSIST_VECTORS, pii_detect=_PII_DETECT)",
  "new": "_MEM = open_store(_PATH, embed=_EMB_DOC, embed_query=_EMB_QUERY, embed_id=_EMB_ID, receipts=_RECEIPTS,\n                  receipt_key=_SIGNING[\"key\"], writer_key=_WRITER_KEY,\n                  persist_vectors=_PERSIST_VECTORS, pii_detect=_PII_DETECT)",
  "tests": [
   "tests/test_mcp_observe_recall.py"
  ]
 },
 {
  "name": "the recall hook loses its encoding guard (cp1250 deletes the block)",
  "file": "inspeximus/claude_code.py",
  "old": "    try:\n        sys.stdout.reconfigure(errors=\"replace\")\n    except Exception:\n        pass\n",
  "new": "",
  "tests": [
   "tests/test_claude_code_recall_surfaces_decisions.py"
  ]
 },
 {
  "name": "the recall hook stops reading the external decision store",
  "file": "inspeximus/claude_code.py",
  "old": "    ext = (os.environ.get(\"INSPEXIMUS_DECISION_STORE\") or \"\").strip()",
  "new": "    ext = \"\"",
  "tests": [
   "tests/test_claude_code_recall_surfaces_decisions.py"
  ]
 },
 {
  "name": "the recall block drops its per-record bound",
  "file": "inspeximus/claude_code.py",
  "old": "        out += [f\"  * {_injected(d['text'], 480)}\" for d in decisions]",
  "new": "        out += [f\"  * {d['text']}\" for d in decisions]",
  "tests": [
   "tests/test_claude_code_recall_surfaces_decisions.py"
  ]
 },
 {
  "name": "index.html JSON-LD softwareVersion stops being pinned",
  "file": "packages/_pin_server_json.py",
  "old": "        new, n = re.subn(r'(\"softwareVersion\":\\s*\")[^\"]*(\")', rf'\\g<1>{version}\\g<2>', text, count=1)",
  "new": "        new, n = text, 1",
  "tests": [
   "tests/test_manifest_versions.py"
  ]
 },
 {
  "name": "the index.html hero eyebrow stops being pinned",
  "file": "packages/_pin_server_json.py",
  "old": "        new, m = re.subn(r'(<p class=\"eyebrow\">)v\\d+\\.\\d+\\.\\d+(\\s)', rf'\\g<1>v{version}\\g<2>', new, count=1)",
  "new": "        m = 1",
  "tests": [
   "tests/test_manifest_versions.py"
  ]
 },
 {
  "name": "the no-tenant fallback comes back for our own key (unbound rows promotable)",
  "file": "inspeximus/core.py",
  "old": "for cand in ((_tn,) if (_tn is None or key in nameable) else (_tn, None)):",
  "new": "for cand in ((_tn,) if _tn is None else (_tn, None)):",
  "tests": [
   "tests/test_writes_can_carry_their_own_key.py"
  ]
 },
 {
  "name": "reserved meta keys reach the record again (top tier askable)",
  "file": "inspeximus/core.py",
  "old": "                    if k not in _RESERVED_META and k not in _META_ALIASED_PARAM}",
  "new": "                    if k not in _META_ALIASED_PARAM}",
  "tests": [
   "tests/test_isolation_and_persistence.py"
  ]
 },
 {
  "name": "the tenant view forwards _stamp to the parent (internal writes unscoped)",
  "file": "inspeximus/core.py",
  "old": "        return Inspeximus._stamp(self, *a, **k)",
  "new": "        return self._parent._stamp(*a, **k)",
  "tests": [
   "tests/test_session_digest.py"
  ]
 },
 {
  "name": "check_sources calls a store with nothing checkable OK (empty check reads clean)",
  "file": "inspeximus/core.py",
  "old": "            \"ok\": (not drifted and not orphaned and not unbound_capture\n                   and (bool(checked) or (_bindable == 0 and _n > 0))),",
  "new": "            \"ok\": (not drifted and not orphaned and not unbound_capture),",
  "tests": [
   "tests/test_isolation_and_persistence.py"
  ]
 },
 {
  "name": "the source fingerprint goes into the caller-writable source dict instead of reserved meta",
  "file": "inspeximus/core.py",
  "old": "                    rec[\"meta\"][\"source_sha256\"] = _obs.lower() if _obs else _at_write",
  "new": "                    rec[\"source\"][\"sha256\"] = _obs.lower() if _obs else _at_write",
  "tests": [
   "tests/test_isolation_and_persistence.py"
  ]
 },
 {
  "name": "check_sources reads the raw list, leaking other tenants into a tenant's report",
  "file": "inspeximus/core.py",
  "old": "        rows = self.items if project is None else [\n            r for r in self.items if (r.get(\"meta\") or {}).get(\"project\") in (None, str(project))]",
  "new": "        rows = self._items if project is None else [\n            r for r in self._items if (r.get(\"meta\") or {}).get(\"project\") in (None, str(project))]",
  "tests": [
   "tests/test_isolation_and_persistence.py"
  ]
 },
 {
  "name": "the MCP server reads the persist-vectors flag and never passes it to the store",
  "file": "inspeximus/mcp_server.py",
  "old": "_MEM = open_store(_PATH, embed=_EMB_DOC, embed_query=_EMB_QUERY, embed_id=_EMB_ID, receipts=_RECEIPTS,\n                  receipt_key=_SIGNING[\"key\"], observe_recall=_OBSERVE_RECALL, writer_key=_WRITER_KEY,\n                  persist_vectors=_PERSIST_VECTORS, pii_detect=_PII_DETECT)",
  "new": "_MEM = open_store(_PATH, embed=_EMB_DOC, embed_query=_EMB_QUERY, embed_id=_EMB_ID, receipts=_RECEIPTS,\n                  receipt_key=_SIGNING[\"key\"], observe_recall=_OBSERVE_RECALL, writer_key=_WRITER_KEY, pii_detect=_PII_DETECT)",
  "tests": [
   "tests/test_the_server_can_keep_the_vectors_it_paid_for.py"
  ]
 },
 {
  "name": "the MCP server reads the PII flag and never passes it to the store",
  "file": "inspeximus/mcp_server.py",
  "old": "_MEM = open_store(_PATH, embed=_EMB_DOC, embed_query=_EMB_QUERY, embed_id=_EMB_ID, receipts=_RECEIPTS,\n                  receipt_key=_SIGNING[\"key\"], observe_recall=_OBSERVE_RECALL, writer_key=_WRITER_KEY,\n                  persist_vectors=_PERSIST_VECTORS, pii_detect=_PII_DETECT)",
  "new": "_MEM = open_store(_PATH, embed=_EMB_DOC, embed_query=_EMB_QUERY, embed_id=_EMB_ID, receipts=_RECEIPTS,\n                  receipt_key=_SIGNING[\"key\"], observe_recall=_OBSERVE_RECALL, writer_key=_WRITER_KEY,\n                  persist_vectors=_PERSIST_VECTORS)",
  "tests": [
   "tests/test_the_server_can_keep_the_vectors_it_paid_for.py"
  ]
 },
 {
  "name": "the certificate signs a core without the pubkey, so a signature can be re-attributed",
  "file": "inspeximus/erasure_residue.py",
  "old": "cert[\"signature\"] = sk.sign(_canonical(dict(core, pubkey=cert[\"pubkey\"]))).hex()",
  "new": "cert[\"signature\"] = sk.sign(_canonical(core)).hex()",
  "tests": [
   "tests/test_a_certificate_about_a_store_we_do_not_own.py"
  ]
 },
 {
  "name": "the manifest commitment is asserted rather than recomputed",
  "file": "inspeximus/erasure_residue.py",
  "old": "checks[\"manifest_committed\"] = recomputed == cert.get(\"manifest_sha256\")",
  "new": "checks[\"manifest_committed\"] = True",
  "tests": [
   "tests/test_a_certificate_about_a_store_we_do_not_own.py"
  ]
 },
 {
  "name": "a document with a bad signature is still called valid",
  "file": "inspeximus/erasure_residue.py",
  "old": "             and checks.get(\"signature_valid\") is not False",
  "new": "             and checks.get(\"signature_valid\") is not None",
  "tests": [
   "tests/test_a_certificate_about_a_store_we_do_not_own.py"
  ]
 },
 {
  "name": "the manifest records no hash, so nothing commits to the bytes that were searched",
  "file": "inspeximus/erasure_residue.py",
  "old": "files.append({\"path\": rel.replace(os.sep, \"/\"), \"sha256\": hashlib.sha256(blob).hexdigest(),",
  "new": "files.append({\"path\": rel.replace(os.sep, \"/\"), \"sha256\": \"\",",
  "tests": [
   "tests/test_a_certificate_about_a_store_we_do_not_own.py"
  ]
 },
 {
  "name": "re-verification walks with its own parameters instead of the certificate's",
  "file": "inspeximus/erasure_residue.py",
  "old": "    skip = set(params.get(\"skip_dirs\") or [])",
  "new": "    skip = set()",
  "tests": [
   "tests/test_a_certificate_about_a_store_we_do_not_own.py"
  ]
 },
 {
  "name": "drift compares two scans that cannot honestly be differenced",
  "file": "inspeximus/erasure_residue.py",
  "old": "        \"comparable\": not problems,",
  "new": "        \"comparable\": True,",
  "tests": [
   "tests/test_a_certificate_about_a_store_we_do_not_own.py"
  ]
 },
 {
  "name": "the remembered summary carries the whole manifest into the memory store",
  "file": "inspeximus/erasure_residue.py",
  "old": "            \"manifest_files\": len(cert.get(\"manifest\") or []),",
  "new": "            \"manifest_files\": len(cert.get(\"manifest\") or []), \"manifest\": cert.get(\"manifest\"),",
  "tests": [
   "tests/test_a_certificate_about_a_store_we_do_not_own.py"
  ]
 },
 {
  "name": "every engagement is remembered under one key, so clients overwrite each other",
  "file": "inspeximus/core.py",
  "old": "key = key or (\"residue::\" + str(label))",
  "new": "key = key or \"residue\"",
  "tests": [
   "tests/test_a_certificate_about_a_store_we_do_not_own.py"
  ]
 },
 {
  "name": "a possessive keeps its apostrophe and never matches its noun",
  "file": "inspeximus/core.py",
  "old": "    if w.endswith(\"'s\"):\n        w = w[:-2]\n    elif w.endswith(\"'\"):\n        w = w[:-1]\n",
  "new": "",
  "tests": [
   "tests/test_a_possessive_is_the_noun_it_belongs_to.py"
  ]
 },
 {
  "name": "only the trailing apostrophe is folded, so alice's still stems to alice'",
  "file": "inspeximus/core.py",
  "old": "    if w.endswith(\"'s\"):\n        w = w[:-2]\n    elif w.endswith(\"'\"):",
  "new": "    if w.endswith(\"'\"):",
  "tests": [
   "tests/test_a_possessive_is_the_noun_it_belongs_to.py"
  ]
 },
 {
  "file": "inspeximus/core.py",
  "name": "the anchor's tombstone count is never compared to the chain",
  "old": "    if anc.get(\"n_tombstones\") is not None and anc.get(\"n_tombstones\") != len(toms):\n",
  "new": "    if False:\n",
  "tests": [
   "tests/test_a_trimmed_certificate_is_not_a_certificate.py"
  ]
 },
 {
  "file": "inspeximus/core.py",
  "name": "the tombstones_root is never re-derived",
  "old": "            if _mroot(leaves).hex() != anc.get(\"tombstones_root\"):\n",
  "new": "            if False:\n",
  "tests": [
   "tests/test_a_trimmed_certificate_is_not_a_certificate.py"
  ]
 },
 {
  "file": "inspeximus/core.py",
  "name": "a chain shorter than the witnessed one passes",
  "old": "        elif len(toms) < w_n:\n",
  "new": "        elif False:\n",
  "tests": [
   "tests/test_a_trimmed_certificate_is_not_a_certificate.py"
  ]
 },
 {
  "file": "inspeximus/core.py",
  "name": "anchor_witnessed and store_bound are decorative in valid",
  "old": "             and checks[\"anchor_witnessed\"] is not False and checks[\"store_bound\"] is not False\n",
  "new": "",
  "tests": [
   "tests/test_a_trimmed_certificate_is_not_a_certificate.py"
  ]
 },
 {
  "file": "inspeximus/core.py",
  "name": "a stranger's store binds",
  "old": "        elif w_tip != _GENESIS and w_tip not in hashes:\n",
  "new": "        elif False:\n",
  "tests": [
   "tests/test_a_trimmed_certificate_is_not_a_certificate.py"
  ]
 },
 {
  "file": "inspeximus/cli.py",
  "name": "bound actions verify never re-hashes the records",
  "old": "            if getattr(m, \"_receipts\", None):\n                w_ok, w_problems = m.verify_writes(expected_pubkey=a.expected_pubkey)\n",
  "new": "            if getattr(m, \"_receipts\", None):\n                w_ok, w_problems = True, []\n",
  "tests": [
   "tests/test_a_trimmed_certificate_is_not_a_certificate.py"
  ]
 },
 {
  "file": "inspeximus/actions.py",
  "name": "matches mints a salt when the file is missing",
  "old": "        salt = self._salt_bytes(create=False)\n",
  "new": "        salt = self._salt_bytes()\n",
  "tests": [
   "tests/test_a_trimmed_certificate_is_not_a_certificate.py"
  ]
 },
 {
  "file": "inspeximus/cli.py",
  "name": "a BOM on a transcript file reads as an edit or a traceback",
  "old": "                with open(pth, encoding=\"utf-8-sig\") as fh:       # a BOM is not a transcript edit\n",
  "new": "                with open(pth, encoding=\"utf-8\") as fh:\n",
  "tests": [
   "tests/test_a_trimmed_certificate_is_not_a_certificate.py"
  ]
 },
 {
  "file": "inspeximus/core.py",
  "name": "records are written without a nonce",
  "old": "               \"nonce\": os.urandom(16).hex(),\n",
  "new": "",
  "tests": [
   "tests/test_an_erased_records_receipt_cannot_be_guessed.py"
  ]
 },
 {
  "file": "inspeximus/core.py",
  "name": "the nonce is not folded into the content hashes",
  "old": "        if _n:\n            _imm[\"nonce\"] = _con[\"nonce\"] = _val[\"nonce\"] = _n\n",
  "new": "        if False:\n            _imm[\"nonce\"] = _con[\"nonce\"] = _val[\"nonce\"] = _n\n",
  "tests": [
   "tests/test_an_erased_records_receipt_cannot_be_guessed.py"
  ]
 },
 {
  "file": "inspeximus/migrate.py",
  "name": "the mem0 user is not the subject",
  "old": "            source={\"doc\": f\"mem0/user/{user}\"} if user else None,\n",
  "new": "            source=None,\n",
  "tests": [
   "tests/test_a_mem0_export_imports_with_its_subjects.py"
  ]
 },
 {
  "file": "inspeximus/migrate.py",
  "name": "a second import writes every memory again",
  "old": "        if ident in in_store:\n",
  "new": "        if False:\n",
  "tests": [
   "tests/test_a_mem0_export_imports_with_its_subjects.py"
  ]
 },
 {
  "file": "inspeximus/migrate.py",
  "name": "a re-import after an erasure writes the person back",
  "old": "        if _h(ident) in ever:\n",
  "new": "        if False:\n",
  "tests": [
   "tests/test_a_mem0_export_imports_with_its_subjects.py"
  ]
 },
 {
  "file": "inspeximus/migrate.py",
  "name": "an expired mem0 memory is imported by default",
  "old": "        if _expired_for_mem0(it.get(\"expiration_date\"), now) and not include_expired:\n",
  "new": "        if False:\n",
  "tests": [
   "tests/test_a_mem0_export_imports_with_its_subjects.py"
  ]
 },
 {
  "file": "inspeximus/migrate.py",
  "name": "created_at is not the event time",
  "old": "            valid_from=_event_time(it.get(\"created_at\")),\n",
  "new": "",
  "tests": [
   "tests/test_a_mem0_export_imports_with_its_subjects.py"
  ]
 },
 {
  "file": "inspeximus/migrate.py",
  "name": "the metadata key is not namespaced by user",
  "old": "        if key and user and \"::\" not in key:\n",
  "new": "        if False:\n",
  "tests": [
   "tests/test_a_mem0_export_imports_with_its_subjects.py"
  ]
 },
 {
  "file": "inspeximus/migrate.py",
  "name": "mem0's expiry rule becomes 'on or before today'",
  "old": "    return d < datetime.fromtimestamp(now, tz=timezone.utc).date()\n",
  "new": "    return d <= datetime.fromtimestamp(now, tz=timezone.utc).date()\n",
  "tests": [
   "tests/test_a_mem0_export_imports_with_its_subjects.py"
  ]
 },
 {
  "file": "inspeximus/coverage.py",
  "name": "every probe reads as evidence, even on an empty store",
  "old": "            row[\"state\"] = EVIDENCE if n > 0 else CAPABILITY\n",
  "new": "            row[\"state\"] = EVIDENCE\n",
  "tests": [
   "tests/test_coverage_names_what_is_missing.py"
  ]
 },
 {
  "file": "inspeximus/coverage.py",
  "name": "a probe that raises is reported as evidence",
  "old": "                n, detail = 0, f\"probe could not run: {type(e).__name__}: {e}\"[:200]\n",
  "new": "                n, detail = 1, f\"probe could not run: {type(e).__name__}: {e}\"[:200]\n",
  "tests": [
   "tests/test_coverage_names_what_is_missing.py"
  ]
 },
 {
  "file": "inspeximus/coverage.py",
  "name": "not-applicable rows are counted as in scope",
  "old": "    in_scope = counts[EVIDENCE] + counts[CAPABILITY] + counts[NOT_COVERED]\n",
  "new": "    in_scope = len(rows)\n",
  "tests": [
   "tests/test_coverage_names_what_is_missing.py"
  ]
 },
 {
  "file": "inspeximus/coverage.py",
  "name": "the action probe counts nothing",
  "old": "    n = len(_entries(store, \"action\"))\n    signed",
  "new": "    n = 0\n    signed",
  "tests": [
   "tests/test_coverage_names_what_is_missing.py"
  ]
 },
 {
  "file": "inspeximus/actions.py",
  "name": "a risk source outside Art. 9(2) is accepted",
  "old": "        if source not in RISK_SOURCES:\n            raise ValueError(f\"source must be one of {RISK_SOURCES}\")",
  "new": "        if False:\n            raise ValueError(f\"source must be one of {RISK_SOURCES}\")",
  "tests": [
   "tests/test_a_risk_is_a_history_and_monitoring_is_signed_into_the_ledger.py"
  ]
 },
 {
  "file": "inspeximus/actions.py",
  "name": "the register never counts an untested risk",
  "old": "                           \"without_test\": sum(1 for r in open_rows if not r[\"tests\"]),",
  "new": "                           \"without_test\": 0,",
  "tests": [
   "tests/test_a_risk_is_a_history_and_monitoring_is_signed_into_the_ledger.py"
  ]
 },
 {
  "file": "inspeximus/actions.py",
  "name": "the monitoring entry hashes an empty summary",
  "old": "\"summary_sha256\": _content_hash(report),",
  "new": "\"summary_sha256\": _content_hash({}),",
  "tests": [
   "tests/test_a_risk_is_a_history_and_monitoring_is_signed_into_the_ledger.py"
  ]
 },
 {
  "file": "inspeximus/actions.py",
  "name": "the monitoring report is never signed into the ledger",
  "old": "        if actor:\n            extra = {\"since\": since, \"until\": until,",
  "new": "        if False:\n            extra = {\"since\": since, \"until\": until,",
  "tests": [
   "tests/test_a_risk_is_a_history_and_monitoring_is_signed_into_the_ledger.py"
  ]
 },
 {
  "file": "inspeximus/coverage.py",
  "name": "the risk probe counts nothing",
  "old": "    ev = _entries(store, \"risk\")\n    ids",
  "new": "    ev = []\n    ids",
  "tests": [
   "tests/test_a_risk_is_a_history_and_monitoring_is_signed_into_the_ledger.py"
  ]
 },
 {
  "file": "inspeximus/actions.py",
  "name": "a party outside Art. 20 is accepted as informed",
  "old": "            if not isinstance(p, dict) or p.get(\"party\") not in INFORMED_PARTIES:",
  "new": "            if not isinstance(p, dict):",
  "tests": [
   "tests/test_a_correction_a_request_an_explanation_and_a_breach_are_ledger_entries.py"
  ]
 },
 {
  "file": "inspeximus/actions.py",
  "name": "the corrective report never lists a party not informed",
  "old": "                \"not_informed\": [p for p in INFORMED_PARTIES if p not in told],",
  "new": "                \"not_informed\": [],",
  "tests": [
   "tests/test_a_correction_a_request_an_explanation_and_a_breach_are_ledger_entries.py"
  ]
 },
 {
  "file": "inspeximus/actions.py",
  "name": "a late notification to the authority needs no reasons",
  "old": "            if to == \"supervisory_authority\" and when > float(e[\"notify_deadline_ts\"]) and not reasons_for_delay:",
  "new": "            if False:",
  "tests": [
   "tests/test_a_correction_a_request_an_explanation_and_a_breach_are_ledger_entries.py"
  ]
 },
 {
  "file": "inspeximus/actions.py",
  "name": "the breach clock is 24 hours",
  "old": "BREACH_DEADLINE_HOURS = 72",
  "new": "BREACH_DEADLINE_HOURS = 24",
  "tests": [
   "tests/test_a_correction_a_request_an_explanation_and_a_breach_are_ledger_entries.py"
  ]
 },
 {
  "file": "inspeximus/actions.py",
  "name": "the explanation entry hashes an empty document",
  "old": "                     \"evidence\": [self._resolve_ref(seq)], \"manifest_sha256\": _content_hash(doc)}",
  "new": "                     \"evidence\": [self._resolve_ref(seq)], \"manifest_sha256\": _content_hash({})}",
  "tests": [
   "tests/test_a_correction_a_request_an_explanation_and_a_breach_are_ledger_entries.py"
  ]
 },
 {
  "file": "inspeximus/coverage.py",
  "name": "the authority probe counts nothing",
  "old": "    n = len(_entries(store, \"authority\"))",
  "new": "    n = 0",
  "tests": [
   "tests/test_a_correction_a_request_an_explanation_and_a_breach_are_ledger_entries.py"
  ]
 },
 {
  "file": "inspeximus/core.py",
  "name": "the backfill marker is not committed",
  "old": "            if rec.get(\"backfill\"):\n                core[\"backfill\"] = rec[\"backfill\"]",
  "new": "            if False:\n                core[\"backfill\"] = rec[\"backfill\"]",
  "tests": [
   "tests/test_enable_receipts_covers_what_was_written_before_the_chain.py"
  ]
 },
 {
  "file": "inspeximus/core.py",
  "name": "the backfill declares no retirement",
  "old": "        declared = self._unaccounted_retirements()\n        by_id = {r[\"id\"]: r for r in self._items}",
  "new": "        declared = []\n        by_id = {r[\"id\"]: r for r in self._items}",
  "tests": [
   "tests/test_enable_receipts_covers_what_was_written_before_the_chain.py"
  ]
 },
 {
  "file": "inspeximus/core.py",
  "name": "the backfill covers nothing",
  "old": "        uncovered = [r for r in self._items if r[\"id\"] not in named]",
  "new": "        uncovered = []",
  "tests": [
   "tests/test_enable_receipts_covers_what_was_written_before_the_chain.py"
  ]
 },
 {
  "file": "inspeximus/sqlite_store.py",
  "name": "the declared-ids writer emits no events",
  "old": "    if auto_events and (touched or removed):",
  "new": "    if False:",
  "tests": [
   "tests/test_memory_events_land_in_the_same_transaction_as_the_rows.py"
  ]
 },
 {
  "file": "inspeximus/sqlite_store.py",
  "name": "the events are committed before the row transaction opens",
  "old": "        return {\"snapshot\": now, \"added\": 0, \"changed\": 0, \"removed\": 0, \"event_seqs\": []}\n    con = _connect(path)\n    try:",
  "new": "        return {\"snapshot\": now, \"added\": 0, \"changed\": 0, \"removed\": 0, \"event_seqs\": []}\n    con = _connect(path)\n    seqs = _insert_events(con, _ev) if _ev else []\n    _ev = []\n    try:",
  "tests": [
   "tests/test_memory_events_land_in_the_same_transaction_as_the_rows.py"
  ]
 },
 {
  "file": "inspeximus/core.py",
  "name": "an agent-bound view sees every record event",
  "old": "                            if not self.can_read(agent, mid).get(\"allowed\"):\n                                continue",
  "new": "                            if False:\n                                continue",
  "tests": [
   "tests/test_memory_events_land_in_the_same_transaction_as_the_rows.py"
  ]
 },
 {
  "file": "inspeximus/core.py",
  "name": "a tenant-bound view sees every tenant's events",
  "old": "                if self.tenant is not None and ev.get(\"tenant\") != self.tenant:\n                    continue",
  "new": "                if False:\n                    continue",
  "tests": [
   "tests/test_memory_events_land_in_the_same_transaction_as_the_rows.py"
  ]
 },
 {
  "file": "inspeximus/core.py",
  "name": "the L1 key drops the agent, so a hit crosses agents",
  "old": "        ck = (self.tenant, getattr(self, \"agent\", None), key)",
  "new": "        ck = (self.tenant, None, key)",
  "tests": [
   "tests/test_the_l1_cache_never_serves_across_a_scope_and_never_outlives_a_write.py"
  ]
 },
 {
  "file": "inspeximus/core.py",
  "name": "a keyed write leaves the L1 entry in place",
  "old": "            if key.startswith(_ACL_PREFIX):\n                self._l1_reset()\n            else:\n                self._l1_drop_key(key)",
  "new": "            if key.startswith(_ACL_PREFIX):\n                self._l1_reset()\n            else:\n                pass",
  "tests": [
   "tests/test_the_l1_cache_never_serves_across_a_scope_and_never_outlives_a_write.py"
  ]
 },
 {
  "file": "inspeximus/core.py",
  "name": "a hit is not re-validated against the record's status",
  "old": "        if rec is not None and rec.get(\"status\") == \"active\" and rec.get(\"key\") == key:",
  "new": "        if rec is not None:",
  "tests": [
   "tests/test_the_l1_cache_never_serves_across_a_scope_and_never_outlives_a_write.py"
  ]
 },
 {
  "file": "inspeximus/core.py",
  "name": "a replaced record list keeps the old L1",
  "old": "        self.__dict__[\"_Inspeximus__items\"] = value\n        self._l1_reset()",
  "new": "        self.__dict__[\"_Inspeximus__items\"] = value",
  "tests": [
   "tests/test_the_l1_cache_never_serves_across_a_scope_and_never_outlives_a_write.py"
  ]
 },
 {
  "file": "inspeximus/core.py",
  "name": "retire leaves the record active",
  "old": "        for r in targets:\n            r[\"status\"] = \"superseded\"\n            self._touch(r)\n            r[\"superseded_ts\"] = now\n            r[\"invalidated_at\"] = now\n            meta = r.setdefault(\"meta\", {})\n            meta[\"superseded_by_policy\"] = \"retired\"",
  "new": "        for r in targets:\n            self._touch(r)\n            r[\"superseded_ts\"] = now\n            r[\"invalidated_at\"] = now\n            meta = r.setdefault(\"meta\", {})\n            meta[\"superseded_by_policy\"] = \"retired\"",
  "tests": [
   "tests/test_retire_ends_a_key_with_no_replacement.py"
  ]
 },
 {
  "file": "inspeximus/core.py",
  "name": "retire does not declare itself in the chain",
  "old": "            self._declare_retired(r, f\"retired: {why[:200]}\")\n            ids.append(r[\"id\"])",
  "new": "            ids.append(r[\"id\"])",
  "tests": [
   "tests/test_retire_ends_a_key_with_no_replacement.py"
  ]
 },
 {
  "file": "inspeximus/core.py",
  "name": "retire reaches past the handle's scope",
  "old": "        targets = [r for r in self.items if r.get(\"key\") == k and r.get(\"status\") == \"active\"]",
  "new": "        targets = [r for r in self._items if r.get(\"key\") == k and r.get(\"status\") == \"active\"]",
  "tests": [
   "tests/test_retire_ends_a_key_with_no_replacement.py"
  ]
 },
 {
  "file": "inspeximus/core.py",
  "name": "the lock file is never removed",
  "old": "                if os.name == \"nt\":\n                    try:\n                        self._fh.close()",
  "new": "                if False:\n                    try:\n                        self._fh.close()",
  "tests": [
   "tests/test_the_store_lock_file_does_not_outlive_the_lock_on_windows.py"
  ]
 },
 {
  "file": "inspeximus/core.py",
  "name": "a list of non-records is opened anyway",
  "old": "        if _foreign:\n            raise ValueError(",
  "new": "        if False:\n            raise ValueError(",
  "tests": [
   "tests/test_two_findings_from_the_fixture_corpus.py"
  ]
 },
 {
  "file": "inspeximus/core.py",
  "name": "the disk side keeps vec in the not-persisted comparison",
  "old": "                _disk = {r[\"id\"]: _rows_mod_doc({_k: _v for _k, _v in r.items()\n                                                 if _k != \"vec\" or self._persist_vectors})",
  "new": "                _disk = {r[\"id\"]: _rows_mod_doc({_k: _v for _k, _v in r.items()\n                                                 if True})",
  "tests": [
   "tests/test_two_findings_from_the_fixture_corpus.py"
  ]
 },
 {
  "file": "inspeximus/core.py",
  "name": "an equal authority is held instead of going to the later write",
  "old": "                    if a_new < a_cur:\n                        rec[\"status\"] = \"superseded\"",
  "new": "                    if a_new <= a_cur:\n                        rec[\"status\"] = \"superseded\"",
  "tests": [
   "tests/test_authority_supersession_holds_a_lower_authority_write.py"
  ]
 },
 {
  "file": "inspeximus/core.py",
  "name": "the authority rule ignores reaffirm",
  "old": "        if self.supersession == \"authority\" and not reaffirm:",
  "new": "        if self.supersession == \"authority\":",
  "tests": [
   "tests/test_authority_supersession_holds_a_lower_authority_write.py"
  ]
 },
 {
  "file": "inspeximus/core.py",
  "name": "the laundering rule takes the strongest parent instead of the weakest",
  "old": "                    own = pa if own is None else min(own, pa)",
  "new": "                    own = pa if own is None else max(own, pa)",
  "tests": [
   "tests/test_authority_supersession_holds_a_lower_authority_write.py"
  ]
 },
 {
  "file": "inspeximus/core.py",
  "name": "a missing authority reads as 1.0 instead of taking no part",
  "old": "    a = source.get(\"authority\")\n    if a is None or isinstance(a, bool):\n        return None",
  "new": "    a = source.get(\"authority\")\n    if a is None or isinstance(a, bool):\n        return 1.0",
  "tests": [
   "tests/test_authority_supersession_holds_a_lower_authority_write.py"
  ]
 },
 {
  "file": "inspeximus/core.py",
  "name": "the authority rule runs on the default store",
  "old": "    v = explicit if explicit is not None else os.environ.get(\"INSPEXIMUS_SUPERSESSION\", \"lww\")",
  "new": "    v = explicit if explicit is not None else os.environ.get(\"INSPEXIMUS_SUPERSESSION\", \"authority\")",
  "tests": [
   "tests/test_authority_supersession_holds_a_lower_authority_write.py"
  ]
 },
 {
  "file": "inspeximus/core.py",
  "name": "a garbage authority is stored under the policy",
  "old": "        if self.supersession == \"authority\" and isinstance(source, dict) and \"authority\" in source \\\n                and _declared_authority(source) is None:",
  "new": "        if False:",
  "tests": [
   "tests/test_authority_supersession_holds_a_lower_authority_write.py"
  ]
 },
 {
  "file": "inspeximus/core.py",
  "name": "the held write is not marked, so it becomes the current value",
  "old": "                        rec[\"status\"] = \"superseded\"\n                        self._touch(rec)                       # retired on arrival; the incumbent stands",
  "new": "                        rec[\"status\"] = \"active\"\n                        self._touch(rec)                       # retired on arrival; the incumbent stands",
  "tests": [
   "tests/test_authority_supersession_holds_a_lower_authority_write.py"
  ]
 },
 {
  "file": "inspeximus/actions.py",
  "name": "a scored literacy vocabulary is accepted",
  "old": "        if measure not in LITERACY_MEASURES:\n            raise ValueError(f\"measure must be one of {LITERACY_MEASURES}\")",
  "new": "        if False:\n            raise ValueError(f\"measure must be one of {LITERACY_MEASURES}\")",
  "tests": [
   "tests/test_literacy_attestation_responsibilities_declaration_and_documentation_are_ledger_entries.py"
  ]
 },
 {
  "file": "inspeximus/actions.py",
  "name": "a not_applicable attestation needs no basis",
  "old": "        if statement == \"not_applicable\" and not basis:",
  "new": "        if False:",
  "tests": [
   "tests/test_literacy_attestation_responsibilities_declaration_and_documentation_are_ledger_entries.py"
  ]
 },
 {
  "file": "inspeximus/actions.py",
  "name": "an agreement with no provider is recorded",
  "old": "        if not any(r[\"role\"] in (\"provider\", \"new_provider\", \"product_manufacturer\") for r in rows):",
  "new": "        if False:",
  "tests": [
   "tests/test_literacy_attestation_responsibilities_declaration_and_documentation_are_ledger_entries.py"
  ]
 },
 {
  "file": "inspeximus/actions.py",
  "name": "an Annex VII declaration is recorded without its notified body",
  "old": "            if not isinstance(notified_body, dict) or not notified_body.get(\"name\") or not notified_body.get(\"id\"):",
  "new": "            if False:",
  "tests": [
   "tests/test_literacy_attestation_responsibilities_declaration_and_documentation_are_ledger_entries.py"
  ]
 },
 {
  "file": "inspeximus/actions.py",
  "name": "the CE number may differ from the assessing body's",
  "old": "            if nb and ce.get(\"notified_body_id\") and ce[\"notified_body_id\"] != nb[\"id\"]:",
  "new": "            if False:",
  "tests": [
   "tests/test_literacy_attestation_responsibilities_declaration_and_documentation_are_ledger_entries.py"
  ]
 },
 {
  "file": "inspeximus/actions.py",
  "name": "a retention statement without the technical documentation passes",
  "old": "        for k in (\"technical_documentation\", \"eu_declaration_of_conformity\"):\n            if not seen.get(k, {}).get(\"present\"):",
  "new": "        for k in (\"technical_documentation\", \"eu_declaration_of_conformity\"):\n            if False:",
  "tests": [
   "tests/test_literacy_attestation_responsibilities_declaration_and_documentation_are_ledger_entries.py"
  ]
 },
 {
  "file": "inspeximus/actions.py",
  "name": "the retention period is one year, not ten",
  "old": "        end = placed + DOCUMENTATION_RETENTION_YEARS * 365.25 * 86400.0",
  "new": "        end = placed + 1 * 365.25 * 86400.0",
  "tests": [
   "tests/test_literacy_attestation_responsibilities_declaration_and_documentation_are_ledger_entries.py"
  ]
 },
 {
  "file": "inspeximus/actions.py",
  "name": "the attestation register hides the classes with none",
  "old": "                \"missing\": [c for c in PROHIBITED_PRACTICES if c not in latest], \"rows\": rows,",
  "new": "                \"missing\": [], \"rows\": rows,",
  "tests": [
   "tests/test_literacy_attestation_responsibilities_declaration_and_documentation_are_ledger_entries.py"
  ]
 },
 {
  "file": "inspeximus/core.py",
  "name": "the objection filter withholds by canonical host, so a neighbour vanishes too",
  "old": "                _c, ids, _k = self._resolve_subject(o[\"subject\"], allow_ambiguous=bool(o.get(\"allow_ambiguous\")),\n                                                   destructive=True)\n            except Exception:\n                continue\n            out.update(ids)",
  "new": "                _c, ids, _k = self._resolve_subject(o[\"subject\"], allow_ambiguous=bool(o.get(\"allow_ambiguous\")),\n                                                   destructive=True)\n            except Exception:\n                continue\n            out.update(r[\"id\"] for r in self.items if _c & Inspeximus._rec_sources(r))",
  "tests": [
   "tests/test_a_notice_an_objection_a_portable_export_and_a_processing_role_are_ledger_entries.py"
  ]
 },
 {
  "file": "inspeximus/core.py",
  "name": "recall ignores standing objections",
  "old": "            _withheld = self._withheld_ids()\n            if _withheld:\n                pool = [r for r in pool if r[\"id\"] not in _withheld]",
  "new": "            _withheld = set()\n            if _withheld:\n                pool = [r for r in pool if r[\"id\"] not in _withheld]",
  "tests": [
   "tests/test_a_notice_an_objection_a_portable_export_and_a_processing_role_are_ledger_entries.py"
  ]
 },
 {
  "file": "inspeximus/core.py",
  "name": "an upheld objection stops withholding",
  "old": "            if o.get(\"status\") not in (\"standing\", \"upheld\") or (self.tenant is not None and o.get(\"tenant\") != self.tenant):",
  "new": "            if o.get(\"status\") not in (\"standing\",) or (self.tenant is not None and o.get(\"tenant\") != self.tenant):",
  "tests": [
   "tests/test_a_notice_an_objection_a_portable_export_and_a_processing_role_are_ledger_entries.py"
  ]
 },
 {
  "file": "inspeximus/core.py",
  "name": "a direct-marketing objection can be overridden",
  "old": "            if row.get(\"ground\") == \"direct_marketing\":\n                raise ValueError(\"an objection to direct marketing (Art. 21(2)) cannot be overridden\")",
  "new": "            if False:\n                raise ValueError(\"an objection to direct marketing (Art. 21(2)) cannot be overridden\")",
  "tests": [
   "tests/test_a_notice_an_objection_a_portable_export_and_a_processing_role_are_ledger_entries.py"
  ]
 },
 {
  "file": "inspeximus/core.py",
  "name": "an override needs no grounds",
  "old": "            if not grounds:\n                raise ValueError(\"overriding an objection needs the compelling legitimate grounds (Art. 21(1))\")",
  "new": "            if False:\n                raise ValueError(\"overriding an objection needs the compelling legitimate grounds (Art. 21(1))\")",
  "tests": [
   "tests/test_a_notice_an_objection_a_portable_export_and_a_processing_role_are_ledger_entries.py"
  ]
 },
 {
  "file": "inspeximus/core.py",
  "name": "the objection is not persisted",
  "old": "        self._objections.append(row)\n        self._flush_objections()\n        return dict(row)",
  "new": "        self._objections.append(row)\n        return dict(row)",
  "tests": [
   "tests/test_a_notice_an_objection_a_portable_export_and_a_processing_role_are_ledger_entries.py"
  ]
 },
 {
  "file": "inspeximus/actions.py",
  "name": "an Art. 14 notice needs no source",
  "old": "            if not source:\n                raise ValueError(\"an Art. 14 notice names the source the data came from (14(2)(f))\")",
  "new": "            if False:\n                raise ValueError(\"an Art. 14 notice names the source the data came from (14(2)(f))\")",
  "tests": [
   "tests/test_a_notice_an_objection_a_portable_export_and_a_processing_role_are_ledger_entries.py"
  ]
 },
 {
  "file": "inspeximus/actions.py",
  "name": "the notice hides the missing items",
  "old": "                 \"missing\": [i for i in allowed if i not in given],",
  "new": "                 \"missing\": [],",
  "tests": [
   "tests/test_a_notice_an_objection_a_portable_export_and_a_processing_role_are_ledger_entries.py"
  ]
 },
 {
  "file": "inspeximus/actions.py",
  "name": "a processor needs no controller",
  "old": "            if not controller:\n                raise ValueError(f\"a {role} names the controller it acts for\")",
  "new": "            if False:\n                raise ValueError(f\"a {role} names the controller it acts for\")",
  "tests": [
   "tests/test_a_notice_an_objection_a_portable_export_and_a_processing_role_are_ledger_entries.py"
  ]
 },
 {
  "file": "inspeximus/subject_rights.py",
  "name": "every record is portable, inherited ones included",
  "old": "            row[\"portable\"] = row[\"why\"] == \"direct\"",
  "new": "            row[\"portable\"] = True",
  "tests": [
   "tests/test_a_notice_an_objection_a_portable_export_and_a_processing_role_are_ledger_entries.py"
  ]
 },
 {
  "file": "inspeximus/subject_rights.py",
  "name": "the portability export is logged as a plain export",
  "old": "        event = \"portability\" if basis == \"portability\" else \"export\"",
  "new": "        event = \"export\"",
  "tests": [
   "tests/test_a_notice_an_objection_a_portable_export_and_a_processing_role_are_ledger_entries.py"
  ]
 },
 {
  "file": "inspeximus/core.py",
  "name": "the candidate bound is strict, so pairs exactly on the threshold are never scored",
  "old": "                candidates = [ci for ci, n in shared.items()\n                              if n >= sim_threshold * min(len(rtok), len(cents[ci][\"tok\"]))]",
  "new": "                candidates = [ci for ci, n in shared.items()\n                              if n > sim_threshold * min(len(rtok), len(cents[ci][\"tok\"]))]",
  "tests": [
   "tests/test_the_cluster_pass_is_exact_and_no_longer_retokenises.py"
  ]
 },
 {
  "file": "inspeximus/core.py",
  "name": "candidates are scored in arrival order of the index, not cluster order, so the best-on-tie changes",
  "old": "                candidates.sort()\n            for ci in candidates:",
  "new": "                candidates.reverse()\n            for ci in candidates:",
  "tests": [
   "tests/test_the_cluster_pass_is_exact_and_no_longer_retokenises.py"
  ]
 },
 {
  "file": "inspeximus/core.py",
  "name": "the centroid is tokenised per comparison again",
  "old": "                s = self._similarity(c[\"rec\"][\"text\"], r, c[\"vec\"], c[\"tok\"])",
  "new": "                s = self._similarity(c[\"rec\"][\"text\"], r, c[\"vec\"])",
  "tests": [
   "tests/test_the_cluster_pass_is_exact_and_no_longer_retokenises.py"
  ]
 },
 {
  "file": "inspeximus/core.py",
  "name": "consolidate_clusters re-tokenises the anchor per partner",
  "old": "                avec = self._qvec(a[\"text\"])\n                atok = self._rec_tokens(a)\n                for b in members[i + 1:]:\n                    if b[\"status\"] != \"active\" or b[\"id\"] in a[\"links\"]:\n                        continue\n                    if self._similarity(a[\"text\"], b, avec, atok) >= dup_threshold:",
  "new": "                avec = self._qvec(a[\"text\"])\n                atok = self._rec_tokens(a)\n                for b in members[i + 1:]:\n                    if b[\"status\"] != \"active\" or b[\"id\"] in a[\"links\"]:\n                        continue\n                    if self._similarity(a[\"text\"], b, avec, _tokens(a[\"text\"])) >= dup_threshold:",
  "tests": [
   "tests/test_the_cluster_pass_is_exact_and_no_longer_retokenises.py"
  ]
 },
 {
  "file": "inspeximus/core.py",
  "name": "the verbatim restatement is retired like junk",
  "old": "                _same = [r for r in active if r.get(\"object\") is not None and r.get(\"text\") == rec.get(\"text\")]\n                if _same:",
  "new": "                _same = []\n                if _same:",
  "tests": [
   "tests/test_a_verbatim_restatement_carries_its_lineage_through_the_objectless_guard.py"
  ]
 },
 {
  "file": "inspeximus/core.py",
  "name": "any objectless write inherits the object, junk included",
  "old": "                _same = [r for r in active if r.get(\"object\") is not None and r.get(\"text\") == rec.get(\"text\")]",
  "new": "                _same = [r for r in active if r.get(\"object\") is not None]",
  "tests": [
   "tests/test_a_verbatim_restatement_carries_its_lineage_through_the_objectless_guard.py"
  ]
 },
 {
  "file": "inspeximus/core.py",
  "name": "the objectless guard retires silently again",
  "old": "                        \"blocked\": True, \"policy\": \"objectless_guard\", \"current_id\": active[0][\"id\"],",
  "new": "                        \"blocked\": False, \"policy\": None, \"current_id\": active[0][\"id\"],",
  "tests": [
   "tests/test_a_verbatim_restatement_carries_its_lineage_through_the_objectless_guard.py"
  ]
 },
 {
  "file": "inspeximus/core.py",
  "name": "quarantined records are served anyway",
  "old": "            if not include_quarantined:\n                pool = [r for r in pool if not Inspeximus._is_quarantined(r)]",
  "new": "            if False:\n                pool = [r for r in pool if not Inspeximus._is_quarantined(r)]",
  "tests": [
   "tests/test_two_read_guards_quarantine_instructions_and_demote_stuffing.py"
  ]
 },
 {
  "file": "inspeximus/core.py",
  "name": "a stuffed record keeps its rank",
  "old": "            if len(_clean) != len(scored):\n                scored = _clean + [t for t in scored if Inspeximus._is_stuffed(t[2])]",
  "new": "            if False:\n                scored = _clean + [t for t in scored if Inspeximus._is_stuffed(t[2])]",
  "tests": [
   "tests/test_two_read_guards_quarantine_instructions_and_demote_stuffing.py"
  ]
 },
 {
  "file": "inspeximus/core.py",
  "name": "stuffing needs no share, so a long genuine note is flagged",
  "old": "    if c >= 4 and share >= 0.12:",
  "new": "    if c >= 4:",
  "tests": [
   "tests/test_two_read_guards_quarantine_instructions_and_demote_stuffing.py"
  ]
 },
 {
  "file": "inspeximus/core.py",
  "name": "the concealment shape needs no audience, so 'do not tell Bob' is quarantined (re-anchored to the 3.5.1 wording)",
  "old": "(?:[\\w'-]+\\s+){0,3}?(to\\s+)?(the\\s+)?(user|human|operator|owner|anyone)\\b\", re.I)),",
  "new": "(?:[\\w'-]+\\s+){0,3}?(to\\s+)?(the\\s+)?(\\w+)\\b\", re.I)),",
  "tests": [
   "tests/test_two_read_guards_quarantine_instructions_and_demote_stuffing.py"
  ]
 },
 {
  "file": "inspeximus/core.py",
  "name": "release_quarantine records the actor but does not release",
  "old": "                q[\"released\"] = {\"actor\": actor, \"ts\": time.time(), \"reason\": (str(reason)[:500] if reason else None)}\n                self._touch(r)",
  "new": "                q[\"released_by\"] = {\"actor\": actor, \"ts\": time.time(), \"reason\": (str(reason)[:500] if reason else None)}\n                self._touch(r)",
  "tests": [
   "tests/test_two_read_guards_quarantine_instructions_and_demote_stuffing.py"
  ]
 },
 {
  "file": "inspeximus/core.py",
  "name": "records written before the guards are never assessed at recall",
  "old": "        if self.read_guards:\n            for r in pool:\n                self._assess_read_guards(r)\n            if not include_quarantined:",
  "new": "        if self.read_guards:\n            if not include_quarantined:",
  "tests": [
   "tests/test_two_read_guards_quarantine_instructions_and_demote_stuffing.py"
  ]
 },
 {
  "file": "inspeximus/core.py",
  "name": "the environment switch is ignored",
  "old": "    return os.environ.get(\"INSPEXIMUS_READ_GUARDS\", \"1\") != \"0\"",
  "new": "    return True",
  "tests": [
   "tests/test_two_read_guards_quarantine_instructions_and_demote_stuffing.py"
  ]
 },
 {
  "file": "inspeximus/core.py",
  "name": "the exfiltration shape allows no qualifier, so 'send all customer data to <addr>' passes",
  "old": "                r\"(?:[\\w-]+\\s+){0,2}\"          # up to two qualifiers: 'all CUSTOMER data', 'the SIGNING keys'\n",
  "new": "                r\"(?:[\\w-]+\\s+){0}\"          # up to two qualifiers: 'all CUSTOMER data', 'the SIGNING keys'\n",
  "tests": [
   "tests/test_two_read_guards_quarantine_instructions_and_demote_stuffing.py"
  ]
 },
 {
  "file": "inspeximus/core.py",
  "name": "3.5.1 override without the noun is dropped, so 'disregard everything you were told earlier' is served",
  "old": "                r\"|\\b(ignore|disregard|forget)\\s+(everything|anything|all|what|whatever)\\s+\"\n                r\"(you|you've|i|i've|we|we've)\\s+(were|have been|had been|been|was)?\\s*(told|said|instructed|given|asked)\\b\", re.I)),",
  "new": "                , re.I)),",
  "tests": [
   "tests/test_two_read_guards_quarantine_instructions_and_demote_stuffing.py"
  ]
 },
 {
  "file": "inspeximus/core.py",
  "name": "3.5.1 'new instructions follow' needs a colon again",
  "old": "                r\"|\\b(new|updated|revised|real|actual|secret)\\s+(instructions?|directives?)\\s+(follow\\b|below\\b|are as follows)\", re.I)),",
  "new": "                , re.I)),",
  "tests": [
   "tests/test_two_read_guards_quarantine_instructions_and_demote_stuffing.py"
  ]
 },
 {
  "file": "inspeximus/core.py",
  "name": "3.5.1 the concealment shape allows no object again, so 'do not mention this note to the user' is served",
  "old": "(?:[\\w'-]+\\s+){0,3}?(to\\s+)?(the\\s+)?(user|human|operator|owner|anyone)\\b\", re.I)),",
  "new": "(this\\s+)?(to\\s+)?(the\\s+)?(user|human|operator|owner|anyone)\\b\", re.I)),",
  "tests": [
   "tests/test_two_read_guards_quarantine_instructions_and_demote_stuffing.py"
  ]
 },
 {
  "file": "inspeximus/core.py",
  "name": "3.5.1 the backticked and bare curl orders are dropped from the execution shape",
  "old": "                r\"|\\b(execute|run)\\s+`[^`\\n]*(https?://|\\|\\s*(sh|bash))[^`\\n]*`\"\n                r\"|\\b(execute|run)\\s+(curl|wget)\\s[^\\n]{0,120}?(https?://|\\|\\s*(sh|bash)\\b)\", re.I)),",
  "new": "                , re.I)),",
  "tests": [
   "tests/test_two_read_guards_quarantine_instructions_and_demote_stuffing.py"
  ]
 },
 {
  "file": "inspeximus/core.py",
  "name": "3.5.1 the added exfiltration nouns are dropped, so 'email the customer list to <addr>' is served",
  "old": "                r\"|lists?|database|tables?|contacts?|addresses|emails?|customers?|notes?|transcripts?)\\b\"   # 3.5.1: the nouns a paraphrase reaches for",
  "new": "                r\")\\b\"   # 3.5.1: the nouns a paraphrase reaches for",
  "tests": [
   "tests/test_two_read_guards_quarantine_instructions_and_demote_stuffing.py"
  ]
 },
 {
  "file": "inspeximus/core.py",
  "name": "3.5.1 the concealment object window is unbounded, so 'do not tell Bob about the surprise party' reaches no audience but 'never reveal the user's name to Bob' would be flagged either way; the bounded window is what keeps the test's ordinary sentence ordinary",
  "old": "(?:[\\w'-]+\\s+){0,3}?(to\\s+)?(the\\s+)?(user|human|operator|owner|anyone)\\b\", re.I)),",
  "new": "(?:[\\w'-]+\\s+){0,30}?(to\\s+)?(the\\s+)?(user|human|operator|owner|anyone)\\b\", re.I)),",
  "tests": [
   "tests/test_two_read_guards_quarantine_instructions_and_demote_stuffing.py"
  ]
 },
 {
  "file": "inspeximus/core.py",
  "name": "3.5.1 lineage_dropped is always 0, so a rewrite that dropped 10 anchors reads as clean",
  "old": "                    self.last_write[\"lineage_dropped\"] = _n if (_n and not rec.get(\"derived_from\")) else 0",
  "new": "                    self.last_write[\"lineage_dropped\"] = 0",
  "tests": [
   "tests/test_a_blocked_write_says_so_on_every_surface_and_retire_names_its_status.py"
  ]
 },
 {
  "file": "inspeximus/core.py",
  "name": "3.5.1 previous counts a write the guards retired on arrival as the value this write followed",
  "old": "                         and not ((r.get(\"meta\") or {}).get(\"echo_blocked\")\n                                  or (r.get(\"meta\") or {}).get(\"objectless_blocked\")\n                                  or \"rejected_authority\" in (r.get(\"meta\") or {}))]",
  "new": "                         ]",
  "tests": [
   "tests/test_a_blocked_write_says_so_on_every_surface_and_retire_names_its_status.py"
  ]
 },
 {
  "file": "inspeximus/core.py",
  "name": "3.5.1 retire() stops naming the status it set (re-anchored to the 3.5.2 wording)",
  "old": "                \"status\": \"superseded\", \"policy\": \"retired\",\n                # 3.5.2: the same verdict a write gets; a retirement that did not reach disk is",
  "new": "                # 3.5.2: the same verdict a write gets; a retirement that did not reach disk is",
  "tests": [
   "tests/test_a_blocked_write_says_so_on_every_surface_and_retire_names_its_status.py"
  ]
 },
 {
  "file": "inspeximus/mcp_server.py",
  "name": "3.5.1 the MCP write result reports every write as landed",
  "old": "    out = {\"status\": lw.get(\"status\", \"active\"), \"blocked\": bool(lw.get(\"blocked\")),",
  "new": "    out = {\"status\": \"active\", \"blocked\": False,",
  "tests": [
   "tests/test_a_blocked_write_says_so_on_every_surface_and_retire_names_its_status.py"
  ]
 },
 {
  "file": "inspeximus/cli.py",
  "name": "3.5.1 the CLI exits 0 on a blocked write again (re-anchored to the 3.5.2 wording)",
  "old": "            _flush_or_fail(m)\n            return 3\n        if _lw.get(\"persisted\") is False:",
  "new": "            _flush_or_fail(m)\n        if _lw.get(\"persisted\") is False:",
  "tests": [
   "tests/test_a_blocked_write_says_so_on_every_surface_and_retire_names_its_status.py"
  ]
 },
 {
  "file": "inspeximus/core.py",
  "name": "3.5.1 the merge lets disk win for a row this handle edited again, so a retirement beside a peer append is lost",
  "old": "        if _edited:\n            _kept = 0",
  "new": "        if False:\n            _kept = 0",
  "tests": [
   "tests/test_a_retirement_survives_a_peer_write_that_landed_first.py"
  ]
 },
 {
  "file": "inspeximus/core.py",
  "name": "3.5.1 the edited set is read after the load cleared it, so nothing is kept",
  "old": "        _edited = set(self._touched or ())\n        self._items = []",
  "new": "        _edited = set()\n        self._items = []",
  "tests": [
   "tests/test_a_retirement_survives_a_peer_write_that_landed_first.py"
  ]
 },
 {
  "file": "inspeximus/core.py",
  "name": "3.5.2 a failed save no longer stamps persisted False on last_write",
  "old": "            if isinstance(getattr(self, \"last_write\", None), dict):\n                self.last_write[\"persisted\"] = False\n                self.last_write[\"persist_error\"] = self._persist_error[\"error\"]",
  "new": "            if False:\n                self.last_write[\"persisted\"] = False\n                self.last_write[\"persist_error\"] = self._persist_error[\"error\"]",
  "tests": [
   "tests/test_a_write_the_store_could_not_persist_says_so_and_a_foreign_lock_is_retried.py"
  ]
 },
 {
  "file": "inspeximus/core.py",
  "name": "3.5.2 the row write is never retried on a foreign lock",
  "old": "                if \"locked\" not in str(e).lower() or _n == _tries:",
  "new": "                if True:",
  "tests": [
   "tests/test_a_write_the_store_could_not_persist_says_so_and_a_foreign_lock_is_retried.py"
  ]
 },
 {
  "file": "inspeximus/core.py",
  "name": "3.5.2 the surviving error no longer names the client outside inspeximus",
  "old": "        if _last is not None and \"locked\" in str(_last).lower():\n            raise _sq.OperationalError(",
  "new": "        if False:\n            raise _sq.OperationalError(",
  "tests": [
   "tests/test_a_write_the_store_could_not_persist_says_so_and_a_foreign_lock_is_retried.py"
  ]
 },
 {
  "file": "inspeximus/core.py",
  "name": "3.5.2 retire() reports persisted True whatever the save did",
  "old": "                \"persisted\": self._persist_error is None,",
  "new": "                \"persisted\": True,",
  "tests": [
   "tests/test_a_write_the_store_could_not_persist_says_so_and_a_foreign_lock_is_retried.py"
  ]
 },
 {
  "file": "inspeximus/core.py",
  "name": "3.5.2 the store lock key is case-sensitive again, so two spellings of one path are two locks",
  "old": "        h = hashlib.sha256(os.path.normcase(os.path.abspath(str(path))).encode(\"utf-8\", \"replace\")).hexdigest()[:16]",
  "new": "        h = hashlib.sha256(os.path.abspath(str(path)).encode(\"utf-8\", \"replace\")).hexdigest()[:16]",
  "tests": [
   "tests/test_a_write_the_store_could_not_persist_says_so_and_a_foreign_lock_is_retried.py"
  ]
 },
 {
  "file": "inspeximus/mcp_server.py",
  "name": "3.5.2 the MCP result reports every write as persisted",
  "old": "           \"persisted\": bool(lw.get(\"persisted\", True))}",
  "new": "           \"persisted\": True}",
  "tests": [
   "tests/test_a_write_the_store_could_not_persist_says_so_and_a_foreign_lock_is_retried.py"
  ]
 },
 {
  "file": "inspeximus/cli.py",
  "name": "3.5.2 the CLI exits 0 on a write the store could not persist",
  "old": "            return 4\n        if _lw.get(\"lineage_dropped\"):",
  "new": "        if _lw.get(\"lineage_dropped\"):",
  "tests": [
   "tests/test_a_write_the_store_could_not_persist_says_so_and_a_foreign_lock_is_retried.py"
  ]
 },
 {
  "file": "inspeximus/compliance.py",
  "name": "3.6.0 a receipt that hashes to something else reads verified, so a mutated receipt is invisible",
  "old": "            row[\"status\"] = \"verified\" if digest == r[\"receipt_sha256\"] else \"STALE\"",
  "new": "            row[\"status\"] = \"verified\"",
  "tests": [
   "tests/test_the_two_partial_rows_close_art_15_evidence_rows_and_the_qms_register.py"
  ]
 },
 {
  "file": "inspeximus/compliance.py",
  "name": "3.6.0 the Art. 15 control no longer turns STALE_EVIDENCE on a stale row",
  "old": "            if robustness[\"stale\"]:\n                row[\"status\"] = \"STALE_EVIDENCE\"",
  "new": "            if False:\n                row[\"status\"] = \"STALE_EVIDENCE\"",
  "tests": [
   "tests/test_the_two_partial_rows_close_art_15_evidence_rows_and_the_qms_register.py"
  ]
 },
 {
  "file": "inspeximus/compliance.py",
  "name": "3.6.0 the evidence rows land on every Art. 15 control including GDPR's",
  "old": "        if art == \"Art. 15\" and framework.startswith(\"EU AI Act\"):",
  "new": "        if art == \"Art. 15\":",
  "tests": [
   "tests/test_the_two_partial_rows_close_art_15_evidence_rows_and_the_qms_register.py"
  ]
 },
 {
  "file": "inspeximus/coverage.py",
  "name": "3.6.0 the coverage probe for Art. 15 ignores a STALE row",
  "old": "    if ev[\"stale\"]:\n        return 0, f\"{len(ev['stale'])} of {n_rows} robustness evidence row(s) STALE: {', '.join(ev['stale'])}\"",
  "new": "    if False:\n        return 0, f\"{len(ev['stale'])} of {n_rows} robustness evidence row(s) STALE: {', '.join(ev['stale'])}\"",
  "tests": [
   "tests/test_the_two_partial_rows_close_art_15_evidence_rows_and_the_qms_register.py"
  ]
 },
 {
  "file": "inspeximus/actions.py",
  "name": "3.6.0 the qms register never reports a review as overdue",
  "old": "                         \"overdue\": bool(due is not None and float(due) < now),",
  "new": "                         \"overdue\": False,",
  "tests": [
   "tests/test_the_two_partial_rows_close_art_15_evidence_rows_and_the_qms_register.py"
  ]
 },
 {
  "file": "inspeximus/actions.py",
  "name": "3.6.0 the qms register reports the first entry per procedure, not the latest",
  "old": "        for e in ev:\n            latest[e.get(\"procedure\")] = e",
  "new": "        for e in ev:\n            latest.setdefault(e.get(\"procedure\"), e)",
  "tests": [
   "tests/test_the_two_partial_rows_close_art_15_evidence_rows_and_the_qms_register.py"
  ]
 },
 {
  "file": "inspeximus/actions.py",
  "name": "3.6.0 a qms entry accepts any aspect letter",
  "old": "        if aspect is not None and aspect not in QMS_ASPECTS:\n            raise ValueError(f\"aspect must be one of the Art. 17(1) letters {sorted(QMS_ASPECTS)}\")",
  "new": "        if False:\n            raise ValueError(f\"aspect must be one of the Art. 17(1) letters {sorted(QMS_ASPECTS)}\")",
  "tests": [
   "tests/test_the_two_partial_rows_close_art_15_evidence_rows_and_the_qms_register.py"
  ]
 },
 {
  "file": "inspeximus/actions.py",
  "name": "3.6.0 a qms entry accepts an empty owner",
  "old": "        if not owner or not str(owner).strip():\n            raise ValueError(\"a qms record names the procedure's owner\")",
  "new": "        if False:\n            raise ValueError(\"a qms record names the procedure's owner\")",
  "tests": [
   "tests/test_the_two_partial_rows_close_art_15_evidence_rows_and_the_qms_register.py"
  ]
 },
 {
  "file": "inspeximus/deployer.py",
  "name": "3.6.0 the deployer report drops the qms register",
  "old": "            \"evidence\": (_safe(lambda: ledger.qms_register(now=now),\n                               default={\"procedures\": 0, \"overdue\": [], \"rows\": []})\n                         if ledger is not None else {\"procedures\": 0, \"overdue\": [], \"rows\": []}),",
  "new": "            \"evidence\": {\"procedures\": 0, \"overdue\": [], \"rows\": []},",
  "tests": [
   "tests/test_the_two_partial_rows_close_art_15_evidence_rows_and_the_qms_register.py"
  ]
 },
 {
  "file": "inspeximus/core.py",
  "name": "3.6.0 an out-of-band declaration is accepted for a record that is still in the store",
  "old": "        if any(r.get(\"id\") == mid for r in self._items):\n            raise ValueError(f\"{mid} is still in the store; erase it with forget(), which writes its own tombstone\")",
  "new": "        if False:\n            raise ValueError(f\"{mid} is still in the store; erase it with forget(), which writes its own tombstone\")",
  "tests": [
   "tests/test_the_two_partial_rows_close_art_15_evidence_rows_and_the_qms_register.py"
  ]
 },
 {
  "file": "inspeximus/core.py",
  "name": "3.6.0 an out-of-band declaration is accepted for an id no receipt names",
  "old": "        if not any(r.get(\"memory_id\") == mid for r in (self._receipts or [])):\n            raise ValueError(f\"no write receipt names {mid}, so there is nothing for the chain to account for\")",
  "new": "        if False:\n            raise ValueError(f\"no write receipt names {mid}, so there is nothing for the chain to account for\")",
  "tests": [
   "tests/test_the_two_partial_rows_close_art_15_evidence_rows_and_the_qms_register.py"
  ]
 },
 {
  "file": "inspeximus/core.py",
  "name": "3.6.0 the out-of-band tombstone no longer says it was out of band",
  "old": "                                 basis=f\"out_of_band: {str(reason).strip()[:300]}\", authorized_by=str(actor).strip())",
  "new": "                                 basis=f\"declared: {str(reason).strip()[:300]}\", authorized_by=str(actor).strip())",
  "tests": [
   "tests/test_the_two_partial_rows_close_art_15_evidence_rows_and_the_qms_register.py"
  ]
 },
 {
  "file": "inspeximus/scrapi.py",
  "name": "3.6.1 the leaf endpoint serves the receipt instead of the leaf",
  "old": "                if want_leaf:\n                    return self._send(200, \"application/json\", service.entry_leaf(index))",
  "new": "                if False:\n                    return self._send(200, \"application/json\", service.entry_leaf(index))",
  "tests": [
   "tests/test_the_service_speaks_the_wire_protocol.py"
  ]
 },
 {
  "file": "inspeximus/scrapi.py",
  "name": "3.6.1 the leaf endpoint serves the wrong entry's leaf",
  "old": "                    return self._send(200, \"application/json\", service.entry_leaf(index))",
  "new": "                    return self._send(200, \"application/json\", service.entry_leaf(0))",
  "tests": [
   "tests/test_the_service_speaks_the_wire_protocol.py"
  ]
 },
 {
  "file": "tools/publish_static_log.py",
  "name": "3.6.1 a keyless publish asks the no-op signer for receipts again and dies",
  "old": "            try:\n                receipt = service.receipt_for(i) if service.service_pubkey else None\n            except RuntimeError as e:\n                if \"unsigned receipt\" not in str(e):\n                    raise\n                receipt = None",
  "new": "            receipt = service.receipt_for(i)",
  "tests": [
   "tests/test_a_log_a_stranger_can_check_without_us.py"
  ]
 },
 {
  "name": "assessor memo: a constant signature never sees a write",
  "file": "inspeximus/assessor_pack.py",
  "old": "key = (_name, repr(a), repr(sorted(k.items())), _state_signature(store))",
  "new": "key = (_name, repr(a), repr(sorted(k.items())))",
  "tests": [
   "tests/test_the_assessor_pack_computes_each_report_once.py"
  ]
 },
 {
  "name": "assessor memo: every call computes, nothing is reused",
  "file": "inspeximus/assessor_pack.py",
  "old": "            if key in cache:\n",
  "new": "            if False:\n",
  "tests": [
   "tests/test_the_assessor_pack_computes_each_report_once.py"
  ]
 },
 {
  "name": "assessor memo: a reused report is the shared object",
  "file": "inspeximus/assessor_pack.py",
  "old": "            return copy.deepcopy(cache[key])",
  "new": "            return cache[key]",
  "tests": [
   "tests/test_the_assessor_pack_computes_each_report_once.py"
  ]
 },
 {
  "name": "assessor memo: the cache outlives the pack",
  "file": "inspeximus/assessor_pack.py",
  "old": "                delattr(store, name)",
  "new": "                pass",
  "tests": [
   "tests/test_the_assessor_pack_computes_each_report_once.py"
  ]
 },
 {
  "name": "assessor memo: read-path fields are part of the signature",
  "file": "inspeximus/assessor_pack.py",
  "old": "        row = {k: v for k, v in r.items() if not str(k).startswith(\"_\")}",
  "new": "        row = dict(r)",
  "tests": [
   "tests/test_the_assessor_pack_computes_each_report_once.py"
  ]
 },
 {
  "name": "scrapi: the notes file is read and then dropped",
  "file": "inspeximus/scrapi.py",
  "old": "                              notes=notes)",
  "new": "                              notes=\"\")",
  "tests": [
   "tests/test_a_successor_log_records_its_predecessor_in_entry_zero.py"
  ]
 },
 {
  "name": "sender: the cosigned size is never remembered",
  "file": "tools/send_to_witnesses.py",
  "old": "                           \"cosigned_utc\": time.strftime(\"%Y-%m-%dT%H:%M:%SZ\", time.gmtime())}\n        _save(state_path, state)",
  "new": "                           \"cosigned_utc\": time.strftime(\"%Y-%m-%dT%H:%M:%SZ\", time.gmtime())}\n        pass",
  "tests": [
   "tests/test_the_log_sends_its_checkpoint_to_its_witnesses.py"
  ]
 },
 {
  "name": "sender: every send starts from zero",
  "file": "tools/send_to_witnesses.py",
  "old": "        old = int(state.get(w[\"url\"], {}).get(\"size\", 0))",
  "new": "        old = 0",
  "tests": [
   "tests/test_the_log_sends_its_checkpoint_to_its_witnesses.py"
  ]
 },
 {
  "name": "sender: a 409 is not recovered",
  "file": "tools/send_to_witnesses.py",
  "old": "                state[w[\"url\"]][\"size\"] = int(theirs)",
  "new": "                pass",
  "tests": [
   "tests/test_the_log_sends_its_checkpoint_to_its_witnesses.py"
  ]
 },
 {
  "name": "sender: a current witness is sent to again",
  "file": "tools/send_to_witnesses.py",
  "old": "        if old == size:",
  "new": "        if False:",
  "tests": [
   "tests/test_the_log_sends_its_checkpoint_to_its_witnesses.py"
  ]
 },
 {
  "name": "sender: the hourly rate is not enforced",
  "file": "tools/send_to_witnesses.py",
  "old": "        if last is not None and now_ts - float(last) < min_interval:",
  "new": "        if False:",
  "tests": [
   "tests/test_the_log_sends_its_checkpoint_to_its_witnesses.py"
  ]
 },
 {
  "name": "sender: a refused request does not count against the rate",
  "file": "tools/send_to_witnesses.py",
  "old": "        state.setdefault(w[\"url\"], {})[\"attempted_ts\"] = now_ts",
  "new": "        pass",
  "tests": [
   "tests/test_the_log_sends_its_checkpoint_to_its_witnesses.py"
  ]
 },
 {
  "name": "remember: raise_on_block raises on a blocked keyed write",
  "file": "inspeximus/core.py",
  "old": "            raise WriteBlocked(self.last_write)",
  "new": "            pass",
  "tests": [
   "tests/test_raise_on_block_turns_a_blocked_write_into_an_error.py"
  ]
 },
 {
  "name": "pack: the read guards assess every record before the first document",
  "file": "inspeximus/assessor_pack.py",
  "old": "            store.read_guard_report()",
  "new": "            pass",
  "tests": [
   "tests/test_the_assessor_pack_computes_each_report_once.py"
  ]
 },
 {
  "name": "certificate: a partly signed chain no longer fails the verdict",
  "file": "inspeximus/core.py",
  "old": "            sigs_ok = False\n            checks[\"signatures_valid\"] = False\n",
  "new": "            checks[\"signatures_valid\"] = False\n",
  "tests": [
   "tests/test_a_partly_signed_certificate_does_not_verify.py"
  ]
 },
 {
  "name": "cli: UTF-8 consoles keep strict errors, so a lone surrogate crashes the print",
  "file": "inspeximus/cli.py",
  "old": "            if getattr(stream, \"errors\", None) != \"backslashreplace\":",
  "new": "            if (getattr(stream, \"encoding\", \"\") or \"\").lower() not in (\"utf-8\", \"utf8\"):",
  "tests": [
   "tests/test_a_partly_signed_certificate_does_not_verify.py"
  ]
 },
 {
  "name": "readme_blocks: an install a shell block uses itself declares for the next Python block again",
  "file": "tools/readme_blocks.py",
  "old": "    return first is not None and any(",
  "new": "    return False and first is not None and any(",
  "tests": [
   "tests/test_readme_blocks_run.py"
  ]
 },
 {
  "name": "verify_writes: two records with one id are no longer reported",
  "file": "inspeximus/core.py",
  "old": "        for _rid in sorted(k for k, n in _id_count.items() if n > 1):\n",
  "new": "        for _rid in sorted(k for k, n in _id_count.items() if n > 2):\n",
  "tests": [
   "tests/test_verify_writes_sees_what_the_store_serves.py"
  ]
 },
 {
  "name": "verify_writes: an erased record back in the store is no longer reported",
  "file": "inspeximus/core.py",
  "old": "        for _rid in sorted(k for k in _id_count if k in _tombed):\n",
  "new": "        for _rid in sorted(k for k in _id_count if k in _tombed and not k):\n",
  "tests": [
   "tests/test_verify_writes_sees_what_the_store_serves.py"
  ]
 },
 {
  "name": "verify_writes: a chain signed by two keys is no longer reported unpinned",
  "file": "inspeximus/core.py",
  "old": "            if len(_keys) > 1 and not expected_pubkey:\n",
  "new": "            if len(_keys) > 2 and not expected_pubkey:\n",
  "tests": [
   "tests/test_verify_writes_sees_what_the_store_serves.py"
  ]
 },
 {
  "name": "verify_writes: a signed receipt with no backend is called unsigned again",
  "file": "inspeximus/core.py",
  "old": "            elif expected_pubkey and \"sig\" in r:\n",
  "new": "            elif expected_pubkey and \"sig\" in r and False:\n",
  "tests": [
   "tests/test_verify_writes_sees_what_the_store_serves.py"
  ]
 },
 {
  "name": "verify_writes: a signed tombstone with no backend is called unsigned again",
  "file": "inspeximus/core.py",
  "old": "            elif expected_pubkey and \"sig\" in t:\n",
  "new": "            elif expected_pubkey and \"sig\" in t and False:\n",
  "tests": [
   "tests/test_verify_writes_sees_what_the_store_serves.py"
  ]
 },
 {
  "name": "certificate: tombstones signed by two keys verify again",
  "file": "inspeximus/core.py",
  "old": "    if len(_tkeys) > 1:\n",
  "new": "    if len(_tkeys) > 2:\n",
  "tests": [
   "tests/test_a_certificate_verifies_only_as_issued.py"
  ]
 },
 {
  "name": "certificate: a missing scope statement passes as not checked",
  "file": "inspeximus/core.py",
  "old": "    elif _scope_txt is None:\n",
  "new": "    elif _scope_txt is None and False:\n",
  "tests": [
   "tests/test_a_certificate_verifies_only_as_issued.py"
  ]
 },
 {
  "name": "certificate: a missing scope list is skipped",
  "file": "inspeximus/core.py",
  "old": "        if got is None:\n            checks[\"scope_intact\"] = False\n",
  "new": "        if got is None:\n            continue\n",
  "tests": [
   "tests/test_a_certificate_verifies_only_as_issued.py"
  ]
 },
 {
  "name": "certificate: a failed issuer self_check no longer fails the verdict",
  "file": "inspeximus/core.py",
  "old": "             and checks[\"issuer_self_check\"] is not False\n",
  "new": "\n",
  "tests": [
   "tests/test_a_certificate_verifies_only_as_issued.py"
  ]
 },
 {
  "name": "page: tombstones signed by two keys verify again",
  "file": "docs/verify/index.html",
  "old": "  if (tkeys.size > 1) {\n",
  "new": "  if (tkeys.size > 2) {\n",
  "tests": [
   "tests/test_a_certificate_verifies_only_as_issued.py"
  ]
 },
 {
  "name": "page: a missing scope statement passes as not checked",
  "file": "docs/verify/index.html",
  "old": "  } else if (scopeTxt === null) {\n",
  "new": "  } else if (scopeTxt === null && false) {\n",
  "tests": [
   "tests/test_a_certificate_verifies_only_as_issued.py"
  ]
 },
 {
  "name": "page: a failed issuer self_check no longer fails the verdict",
  "file": "docs/verify/index.html",
  "old": "                checks.issuer_self_check !== false &&\n",
  "new": "",
  "tests": [
   "tests/test_a_certificate_verifies_only_as_issued.py"
  ]
 },
 {
  "name": "ledger: an unreadable file is overwritten again",
  "file": "inspeximus/actions.py",
  "old": "        if self._load_error:\n            raise LedgerUnreadable(",
  "new": "        if self._load_error and False:\n            raise LedgerUnreadable(",
  "tests": [
   "tests/test_a_lost_history_is_not_a_fresh_start.py",
   "tests/test_mcp_review_ledger.py"
  ]
 },
 {
  "name": "ledger: verify is silent on an unreadable file",
  "file": "inspeximus/actions.py",
  "old": "        if self._load_error:\n            return False, [f\"the ledger file could not be read",
  "new": "        if self._load_error and False:\n            return False, [f\"the ledger file could not be read",
  "tests": [
   "tests/test_a_lost_history_is_not_a_fresh_start.py",
   "tests/test_mcp_review_ledger.py"
  ]
 },
 {
  "name": "witness: an unreadable state is a first run again",
  "file": "inspeximus/witness_checkpoint.py",
  "old": "        except FileNotFoundError:\n",
  "new": "        except Exception:\n",
  "tests": [
   "tests/test_a_lost_history_is_not_a_fresh_start.py"
  ]
 },
 {
  "name": "verify_inclusion: falls back to the bundle's own root again",
  "file": "inspeximus/core.py",
  "old": "        if not expected_root:\n            return False\n",
  "new": "        if not expected_root:\n            expected_root = bundle.get(\"root\")\n",
  "tests": [
   "tests/test_a_lost_history_is_not_a_fresh_start.py"
  ]
 },
 {
  "name": "mcp: the server finds the store's receipt key and never passes it to the store",
  "file": "inspeximus/mcp_server.py",
  "old": "                  receipt_key=_SIGNING[\"key\"], observe_recall=_OBSERVE_RECALL,",
  "new": "                  observe_recall=_OBSERVE_RECALL,",
  "tests": [
   "tests/test_mcp_review_writes.py",
   "tests/test_mcp_review_erasure.py",
   "tests/test_the_mcp_server_signs_with_the_stores_key.py"
  ]
 },
 {
  "name": "mcp: a key that did not sign the store is used anyway, so the chain is signed by two keys",
  "file": "inspeximus/mcp_server.py",
  "old": "    if signers and pub not in signers:\n",
  "new": "    if False:\n",
  "tests": [
   "tests/test_the_mcp_server_signs_with_the_stores_key.py"
  ]
 },
 {
  "name": "mcp: a configured key on an unsigned chain is used anyway, so the chain is signed in places",
  "file": "inspeximus/mcp_server.py",
  "old": "        if source != \"key home\":\n            raise ReceiptKeyError(",
  "new": "        if False:\n            raise ReceiptKeyError(",
  "tests": [
   "tests/test_the_mcp_server_signs_with_the_stores_key.py"
  ]
 },
 {
  "name": "mcp: a rights tool builds its own keyless ledger again",
  "file": "inspeximus/mcp_server.py",
  "old": "        return _export(_MEM, subject, ledger=_ledger(),",
  "new": "        return _export(_MEM, subject, ledger=__import__(\"inspeximus.actions\", fromlist=[\"x\"]).ActionLedger(_MEM, actor=_ACTOR),",
  "tests": [
   "tests/test_mcp_review_rights_access.py"
  ]
 },
 {
  "name": "mcp: the ledger takes the receipt key over the writer key that already signs it",
  "file": "inspeximus/mcp_server.py",
  "old": "            if signed_by and pub(keys[0]) not in signed_by and pub(keys[1]) in signed_by:\n",
  "new": "            if False:\n",
  "tests": [
   "tests/test_the_mcp_server_signs_with_the_stores_key.py"
  ]
 },
 {
  "name": "ledger: a file that is not JSON loads as an empty chain again",
  "file": "inspeximus/actions.py",
  "old": "                self._load_error = f\"cannot read {self.path}: {type(e).__name__}: {str(e)[:160]}\"\n",
  "new": "                pass\n",
  "tests": [
   "tests/test_a_lost_history_is_not_a_fresh_start.py",
   "tests/test_mcp_review_ledger.py"
  ]
 },
 {
  "name": "ledger: action() refuses an unreadable ledger only after the block has run",
  "file": "inspeximus/actions.py",
  "old": "        self.require_readable()\n        ctx = ActionContext(self, action, inputs, meta)\n",
  "new": "        ctx = ActionContext(self, action, inputs, meta)\n",
  "tests": [
   "tests/test_the_mcp_server_signs_with_the_stores_key.py"
  ]
 },
 {
  "name": "rights: a rectification lands before the ledger is checked",
  "file": "inspeximus/subject_rights.py",
  "old": "    _ledger_can_take_the_entry(ledger)\n    prev = None\n",
  "new": "    prev = None\n",
  "tests": [
   "tests/test_the_mcp_server_signs_with_the_stores_key.py"
  ]
 }
]
