[
  {
    "name": "route()'s default policy drifts from safe to trusting",
    "file": "inspeximus/core.py",
    "old": "policy: str = \"safe\"",
    "new": "policy: str = \"trusting\"",
    "tests": [
      "tests/test_echo_policy_panel.py"
    ]
  },
  {
    "name": "the docstring number is edited away from the measurement",
    "file": "inspeximus/core.py",
    "old": "1.00 echo-blocked",
    "new": "0.50 echo-blocked",
    "tests": [
      "tests/test_echo_policy_panel.py"
    ]
  },
  {
    "name": "the citation points back at the unrunnable probe",
    "file": "inspeximus/core.py",
    "old": "probes/echo_policy_panel.py",
    "new": "probes/echo_attack_probe_v2.py",
    "tests": [
      "tests/test_echo_policy_panel.py"
    ]
  },
  {
    "name": "the echo guard stops blocking a replayed superseded value",
    "file": "probes/echo_policy_panel.py",
    "old": "m.echo_guard = True",
    "new": "m.echo_guard = False",
    "tests": [
      "tests/test_echo_policy_panel.py"
    ]
  },
  {
    "name": "the content check stops seeing mismatches",
    "file": "inspeximus/audit_bundle.py",
    "old": "mismatched = b.get(\"mismatched\") or []",
    "new": "mismatched = []",
    "tests": [
      "tests/test_verify_bundle_content_scope.py"
    ]
  },
  {
    "name": "the verdict claims content was checked when it was not",
    "file": "inspeximus/audit_bundle.py",
    "old": "content_checked = store_items is not None",
    "new": "content_checked = True",
    "tests": [
      "tests/test_verify_bundle_content_scope.py"
    ]
  },
  {
    "name": "ordinary growth is escalated into a failure (false alarm)",
    "file": "inspeximus/audit_bundle.py",
    "old": "limits.append(f\"record {mid} is covered by no receipt in this bundle (written after it was \"",
    "new": "bad(f\"record {mid} is covered by no receipt in this bundle (written after it was \"",
    "tests": [
      "tests/test_verify_bundle_content_scope.py"
    ]
  },
  {
    "name": "the CLI stops printing the scope next to the verdict",
    "file": "inspeximus/audit_bundle.py",
    "old": "    for lim in res.get(\"limits\") or []:\n        print(f\"  NOTE {lim}\")",
    "new": "    for lim in []:\n        print(f\"  NOTE {lim}\")",
    "tests": [
      "tests/test_verify_bundle_content_scope.py"
    ]
  },
  {
    "name": "the headline CLI stops passing the store through",
    "file": "inspeximus/cli.py",
    "old": "res = verify_bundle(bundle, witnesses=wl, threshold=a.threshold, store_items=items)",
    "new": "res = verify_bundle(bundle, witnesses=wl, threshold=a.threshold)",
    "tests": [
      "tests/test_verify_bundle_content_scope.py"
    ]
  },
  {
    "name": "a mistyped store path is created instead of refused",
    "file": "inspeximus/cli.py",
    "old": "            if not os.path.exists(a.store):",
    "new": "            if False:",
    "tests": [
      "tests/test_verify_bundle_content_scope.py"
    ]
  },
  {
    "name": "the headline CLI stops printing the scope",
    "file": "inspeximus/cli.py",
    "old": "            for lim in res.get(\"limits\") or []:\n                print(f\"  NOTE {lim}\")",
    "new": "            for lim in []:\n                print(f\"  NOTE {lim}\")",
    "tests": [
      "tests/test_verify_bundle_content_scope.py"
    ]
  },
  {
    "name": "torch creeps back to module level (mechanism arm dies)",
    "file": "probes/agentpoison_influence_gate.py",
    "old": "import json\nimport os\nimport random\nimport sys",
    "new": "import json\nimport os\nimport random\nimport sys\nimport torch",
    "tests": [
      "tests/test_influence_gate_probes.py"
    ]
  },
  {
    "name": "the influence gate stops filtering uncorroborated memory",
    "file": "probes/agentpoison_influence_gate.py",
    "old": "        return [h for h in hits if corroborated(by_id[h[\"id\"]], by_id)]",
    "new": "        return list(hits)",
    "tests": [
      "tests/test_influence_gate_probes.py"
    ]
  },
  {
    "name": "the dense arm becomes the default again",
    "file": "probes/agentpoison_influence_gate.py",
    "old": "    want_dense = \"--dense\" in argv",
    "new": "    want_dense = _have_torch()",
    "tests": [
      "tests/test_influence_gate_probes.py"
    ]
  },
  {
    "name": "the hashing embedder stops being normalised",
    "file": "probes/agentpoison_influence_gate.py",
    "old": "    return [v / norm for v in vec] if norm else vec",
    "new": "    return vec",
    "tests": [
      "tests/test_influence_gate_probes.py"
    ]
  },
  {
    "name": "the census stops seeing module-level guards",
    "file": "tools/skip_census.py",
    "old": "        if \"importorskip\" in seg:",
    "new": "        if False:",
    "tests": [
      "tests/test_skip_census.py"
    ]
  },
  {
    "name": "the census counts by importing (environment-dependent again)",
    "file": "tools/skip_census.py",
    "old": "        absent = [d for d in deps if importlib.util.find_spec(d) is None]",
    "new": "        absent = []",
    "tests": [
      "tests/test_skip_census.py"
    ]
  },
  {
    "name": "the integrations CI job is deleted",
    "file": ".github/workflows/ci.yml",
    "old": "  integrations:",
    "new": "  integrations_disabled:",
    "tests": [
      "tests/test_skip_census.py"
    ]
  },
  {
    "name": "absence of a clash is reported as support again",
    "file": "inspeximus/core.py",
    "old": "            return _restates(text, rec_text or \"\")                          # True, or None for \"cannot tell\"",
    "new": "            return True",
    "tests": [
      "tests/test_verify_claim_undecidable.py"
    ]
  },
  {
    "name": "the restatement test accepts any overlap",
    "file": "inspeximus/core.py",
    "old": "            return True if overlap >= 0.9 else None",
    "new": "            return True",
    "tests": [
      "tests/test_verify_claim_undecidable.py"
    ]
  },
  {
    "name": "the undecidable case is folded back into 'supported'",
    "file": "inspeximus/core.py",
    "old": "            return _out(\"unverifiable\", undecidable.get(\"object\"),",
    "new": "            return _out(\"supported\", undecidable.get(\"object\"),",
    "tests": [
      "tests/test_verify_claim_undecidable.py"
    ]
  },
  {
    "name": "the keyed path stops distinguishing cannot-tell",
    "file": "inspeximus/core.py",
    "old": "            if m is None:\n                # Neither side carries a value, and the claim is not a restatement. The honest answer is\n                # that this store cannot tell -- with the record attached so the caller can judge.",
    "new": "            if False:\n                # disabled",
    "tests": [
      "tests/test_verify_claim_undecidable.py"
    ]
  },
  {
    "name": "a pruned directory goes unreported again",
    "file": "inspeximus/erasure_residue.py",
    "old": "        for d in pruned:",
    "new": "        for d in []:",
    "tests": [
      "tests/test_erasure_residue.py"
    ]
  },
  {
    "name": "skip_dirs goes back to being unioned (caller cannot opt out)",
    "file": "inspeximus/erasure_residue.py",
    "old": "    skip = _SKIP_DIRS if skip_dirs is None else set(skip_dirs)",
    "new": "    skip = set(skip_dirs or ()) | _SKIP_DIRS",
    "tests": [
      "tests/test_erasure_residue.py"
    ]
  },
  {
    "name": "a nonexistent root returns clean again",
    "file": "inspeximus/erasure_residue.py",
    "old": "    if not os.path.isdir(root):",
    "new": "    if False:",
    "tests": [
      "tests/test_erasure_residue.py"
    ]
  },
  {
    "name": "zero comparisons pass again",
    "file": "inspeximus/audit_bundle.py",
    "old": "    return {\"ok\": bool(compared) and not mismatched, \"checked\": compared, \"receipted\": len(first),",
    "new": "    return {\"ok\": not mismatched, \"checked\": compared, \"receipted\": len(first),",
    "tests": [
      "tests/test_verify_bundle_content_scope.py"
    ]
  },
  {
    "name": "'checked' counts receipts instead of comparisons",
    "file": "inspeximus/audit_bundle.py",
    "old": "        compared += 1",
    "new": "        compared += 0",
    "tests": [
      "tests/test_verify_bundle_content_scope.py"
    ]
  },
  {
    "name": "the orphan truncation stops announcing itself",
    "file": "inspeximus/audit_bundle.py",
    "old": "        if len(orph) > 5:",
    "new": "        if False:",
    "tests": [
      "tests/test_verify_bundle_content_scope.py"
    ]
  },
  {
    "name": "unreceipted records go uncounted again",
    "file": "inspeximus/core.py",
    "old": "            if cur.get(\"status\") == \"active\" and mid not in committed:\n                uncommitted.append(mid)",
    "new": "            if False:\n                uncommitted.append(mid)",
    "tests": [
      "tests/test_uncounted_is_not_unchecked.py"
    ]
  },
  {
    "name": "verify_attribution stops failing closed on disabled receipts",
    "file": "inspeximus/core.py",
    "old": "        if not self.receipts_enabled and self.items:\n            # The sibling one call site over",
    "new": "        if False:\n            # The sibling one call site over",
    "tests": [
      "tests/test_uncounted_is_not_unchecked.py"
    ]
  },
  {
    "name": "coverage goes back to comparing two integers",
    "file": "inspeximus/compliance.py",
    "old": "    if require_receipts and n_receipts and uncovered:",
    "new": "    if False:",
    "tests": [
      "tests/test_uncounted_is_not_unchecked.py"
    ]
  },
  {
    "name": "autogen clear() unscopes again (erases the whole store)",
    "file": "inspeximus/integrations/autogen.py",
    "old": "               if r.get(\"status\") == \"active\" and (\n                   self._tag in (r.get(\"tags\") or [])\n                   or (self._source and (r.get(\"source\") or {}).get(\"doc\") == self._source))]",
    "new": "               if r.get(\"status\") == \"active\"]",
    "tests": [
      "tests/test_uncounted_is_not_unchecked.py"
    ]
  },
  {
    "name": "the served value leaves the commitment again",
    "file": "inspeximus/core.py",
    "old": "                \"value_sha256\": _sha256_hex(_canon({\"object\": rec.get(\"object\")})),",
    "new": "                \"value_sha256\": \"constant\",",
    "tests": [
      "tests/test_value_is_committed.py"
    ]
  },
  {
    "name": "verify_writes stops comparing the value",
    "file": "inspeximus/core.py",
    "old": "                              for k in (\"immutable_sha256\", \"mtype\", \"value_sha256\", \"attrib_sha256\")",
    "new": "                              for k in (\"immutable_sha256\", \"mtype\", \"attrib_sha256\")",
    "tests": [
      "tests/test_value_is_committed.py"
    ]
  },
  {
    "name": "the audit bundle stops binding the value",
    "file": "inspeximus/audit_bundle.py",
    "old": "        for field in (\"immutable_sha256\", \"content_sha256\", \"value_sha256\", \"attrib_sha256\"):",
    "new": "        for field in (\"immutable_sha256\", \"content_sha256\", \"attrib_sha256\"):",
    "tests": [
      "tests/test_value_is_committed.py"
    ]
  },
  {
    "name": "legacy receipts are silently left uncovered",
    "file": "inspeximus/core.py",
    "old": "        if value_strict:",
    "new": "        if False:",
    "tests": [
      "tests/test_value_is_committed.py"
    ]
  },
  {
    "name": "a pre-1.82 receipt raises a false alarm on honest data",
    "file": "inspeximus/core.py",
    "old": "                              if k not in forgiven and k in rc)",
    "new": "                              if k not in forgiven)",
    "tests": [
      "tests/test_value_is_committed.py"
    ]
  },
  {
    "name": "recommit stops appending a receipt",
    "file": "inspeximus/core.py",
    "old": "            self._emit_write_receipt(rec)\n            done.append(rec[\"id\"])",
    "new": "            done.append(rec[\"id\"])",
    "tests": [
      "tests/test_value_is_committed.py"
    ]
  },
  {
    "name": "the manifest header is unbound again",
    "file": "inspeximus/deletion_manifest.py",
    "old": "        prev = _header_hash(subject, request_id, basis, authorized_by, [t.name for t in self._targets])",
    "new": "        prev = _GENESIS",
    "tests": [
      "tests/test_deletion_manifest_header.py"
    ]
  },
  {
    "name": "verify stops checking the header seed",
    "file": "inspeximus/deletion_manifest.py",
    "old": "        if first_prev == _GENESIS and first_prev != header:",
    "new": "        if False:",
    "tests": [
      "tests/test_deletion_manifest_header.py"
    ]
  },
  {
    "name": "the legacy escape hatch stops opening",
    "file": "inspeximus/deletion_manifest.py",
    "old": "        return (len(problems) == 0, problems + notes)",
    "new": "        return (len(problems + notes) == 0, problems + notes)",
    "tests": [
      "tests/test_deletion_manifest_header.py"
    ]
  },
  {
    "name": "a pinned key is ignored when verifying signatures",
    "file": "inspeximus/deletion_manifest.py",
    "old": "                if expected_pubkey is not None and e.get(\"pubkey\") != expected_pubkey:",
    "new": "                if False:",
    "tests": [
      "tests/test_deletion_manifest_header.py"
    ]
  },
  {
    "name": "the header hash drops the subject",
    "file": "inspeximus/deletion_manifest.py",
    "old": "    return _sha256({\"subject\": subject, \"request_id\": request_id, \"basis\": basis,",
    "new": "    return _sha256({\"subject\": None, \"request_id\": request_id, \"basis\": basis,",
    "tests": [
      "tests/test_deletion_manifest_header.py"
    ]
  },
  {
    "name": "zero-deps goes back to reading only installed METADATA",
    "file": "claims_audit.py",
    "old": "        pyproject = root.parent / \"pyproject.toml\"",
    "new": "        pyproject = root.parent / \"nope.toml\"",
    "tests": [
      "tests/test_audit_scripts_have_teeth.py"
    ]
  },
  {
    "name": "a skip stops counting against the gate",
    "file": "claims_audit.py",
    "old": "    return ok is None and not auditing_history",
    "new": "    return False",
    "tests": [
      "tests/test_audit_scripts_have_teeth.py"
    ]
  },
  {
    "name": "the secret sweep can be empty again",
    "file": "governance_audit.py",
    "old": "    if not declared:",
    "new": "    if False:",
    "tests": [
      "tests/test_audit_scripts_have_teeth.py"
    ]
  },
  {
    "name": "a declared secret need not appear in the scenario",
    "file": "governance_audit.py",
    "old": "    if absent:",
    "new": "    if False:",
    "tests": [
      "tests/test_audit_scripts_have_teeth.py"
    ]
  },
  {
    "name": "the audit downloads a package that does not exist",
    "file": "governance_audit.py",
    "old": "                        f\"inspeximus=={a.version}\" if a.version else \"inspeximus\",",
    "new": "                        f\"agora-inspeximus=={a.version}\" if a.version else \"agora-inspeximus\",",
    "tests": [
      "tests/test_audit_scripts_have_teeth.py"
    ]
  },
  {
    "name": "remember_decision commits the TOPIC as the value again",
    "file": "inspeximus/core.py",
    "old": "                             mtype=\"procedural\", key=key, object=(decision.strip() or None),",
    "new": "                             mtype=\"procedural\", key=key, object=(topic.strip() if topic else None),",
    "tests": [
      "tests/test_remember_decision_supersedes.py"
    ]
  },
  {
    "name": "the seeded probe drops back to five seeds",
    "file": "probes/identity_gate_supersession_probe.py",
    "old": "SEEDS = 25",
    "new": "SEEDS = 5",
    "tests": [
      "tests/test_probe_gate.py"
    ]
  },
  {
    "name": "the probe stops printing n with the range",
    "file": "probes/identity_gate_supersession_probe.py",
    "old": "          f\"range {min(ung):.3f}-{max(ung):.3f} over seeds={SEEDS}  \"",
    "new": "          f\"range {min(ung):.3f}-{max(ung):.3f}  \"",
    "tests": [
      "tests/test_probe_gate.py"
    ]
  },
  {
    "name": "the manipulation check stops noticing a count collapse",
    "file": "tools/probe_gate.py",
    "old": "        if len(before) != len(after):",
    "new": "        if False:",
    "tests": [
      "tests/test_probe_gate.py"
    ]
  },
  {
    "name": "the spread floor is removed",
    "file": "tools/probe_gate.py",
    "old": "        ok = len(vals) >= min_trials",
    "new": "        ok = True",
    "tests": [
      "tests/test_probe_gate.py"
    ]
  }
]